name: Smoke test video

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke-test:
    name: Render representative frames and short clip
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install CJK fonts
        run: sudo apt-get update && sudo apt-get install -y fonts-noto-cjk

      - name: Verify CJK font
        run: |
          fc-cache -f
          fc-match -f '%{family}\n' 'Noto Sans CJK SC' | grep -q 'Noto Sans CJK'

      - name: Install dependencies
        run: npm ci

      - name: Extract video assets
        run: |
          mkdir -p public/local-assets
          unzip -q assets/claude-code-install-assets.zip -d public/local-assets

      - name: Verify render inputs
        run: |
          test -f public/local-assets/claude-code-install/subtitles/captions.vtt
          test "$(find public/local-assets/claude-code-install/audio -type f -name '*.mp3' | wc -l | tr -d ' ')" -eq 76

      - name: Type-check project
        run: npm run check

      - name: Render representative frames
        run: |
          mkdir -p smoke
          npx remotion still claude-code-install --frame=30 --output=smoke/frame-000030.png
          npx remotion still claude-code-install --frame=5486 --output=smoke/frame-005486.png
          npx remotion still claude-code-install --frame=10900 --output=smoke/frame-010900.png

      - name: Render first 10 seconds
        run: npx remotion render claude-code-install smoke/first-10-seconds.mp4 --frames=0-299

      - name: Upload smoke-test artifacts
        uses: actions/upload-artifact@v6
        with:
          name: claude-code-install-smoke-test
          path: smoke/
          if-no-files-found: error
          retention-days: 7
