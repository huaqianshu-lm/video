# 我是怎么做视频的（三）：一个长 MP3，为什么不够做完整视频

上一篇，我先把画面和视觉节奏确认下来，最后停在进入声音制作的地方。

这一篇开始处理口播。

我一开始以为，TTS 的工作很简单：把口播稿交给工具，拿到一条 MP3，再放进 Remotion 就结束了。

真正做起来以后，我发现一条能播放的 MP3 还不够。后面还有几个问题必须回答：

- 这一句话到底从什么时候开始。
- 这一句属于哪个 Scene。
- 字幕应该在什么时间出现。
- 画面动作应该跟着哪句话发生。
- 如果只改一个技术词，能不能只重新生成这一小段声音。

所以这一篇不只是记录我怎样生成 TTS，而是记录我怎样把「一篇口播稿」整理成一套可以被视频继续使用的声音时间资料。

**看完这一篇，你会拿到：**

- 一套从 Narration Script 到分段音频的制作流程。
- 一个适合 TTS 的 `TTS Script` 结构。
- 一种让音频、字幕和画面始终找到同一句话的方法。
- 一套可以单独替换某个 Segment 的声音资料组织方式。

> 💡 **一句话总结**：TTS 的目标不是得到一条 MP3，而是得到一条有身份、有时间的声音轨道。

## 01 一篇口播稿，怎么变成视频声音

视频里的声音和文章里的文字，不是同一种东西。

文章可以回看，可以跳读，也可以接受一段比较长的解释；口播则要随着画面往前走。观众听到一句话时，画面最好正在展示这句话对应的过程，而不是停在几秒前已经完成的状态。

我把它们之间的关系先画成这样：

```text
Narration Script
        ↓
适合朗读的 TTS Script
        ↓
多个 Segment 音频
        ↓
真实音频时长和 Word Boundary
        ↓
字幕和统一 Timeline
        ↓
Remotion 里的声音、字幕和画面动作
```

这里真正重要的不是「文字变成声音」这一步，而是从声音开始，后面的每个元素都要能找到同一个时间依据。

可以把它理解成一支拍摄团队：

- `Narration Script` 是拍摄前确认的台词。
- Segment ID 是每句台词的场记编号。
- MP3 是演员真正说出来的声音。
- Word Boundary 是台词中每个词出现的时间。
- Timeline Manifest 是整支视频的拍摄通告单。

如果只留下最后合并好的 MP3，就像只拿到一整段拍摄素材，却没有场记、镜头号和时间码。声音当然可以播放，但后面很难精准剪辑和同步。

> 💡 **一句话总结**：声音一旦要驱动画面，就不能只保存声音本身。

## 02 我先确定真正要喂给 TTS 的文字

我没有把原始文章直接交给 TTS，也没有把 `scene-script.md` 原样拿去朗读。

原因很简单：这三份文字解决的问题不同。

|资料|主要服务对象|适合直接朗读吗|
|---|---|---|
|原始文章|读者|不适合，里面有标题、表格和解释结构|
|Scene Script|视频制作过程|不适合，里面有目的、视觉类型和制作备注|
|Narration Script|观众的听觉理解|最接近，但仍要做朗读清理|

进入 TTS 以前，我先逐幕检查口播稿：

- 每个 Scene 有没有明确的口播。
- 口播顺序是否和 14 个 Scene 一致。
- 一句话是不是只表达一个主要意思。
- 画面已经展示的内容，口播还要不要重复。
- 产品名、技术词和数字是否适合直接朗读。
- 句子之间有没有自然的停顿和转折。

例如画面已经出现文件被读取、代码被修改、测试通过，口播就不需要把终端上的每一行日志再念一遍。

我让声音负责解释这个过程意味着什么，让画面负责提供证据：

```text
声音：Claude Code 正在读取项目，确认当前任务涉及哪些文件。
画面：文件树逐项扫描，相关文件出现高亮。
```

如果声音和画面都在重复同一句话，视频会变得很拥挤；如果声音没有解释，画面又只剩几个状态标签，观众仍然不知道这些变化为什么发生。

> 💡 **一句话总结**：先确定观众要听懂什么，再决定 TTS 读哪些文字。

## 03 我第一次用剪映分段生成文字朗读

最开始，我用剪映的文字朗读功能做声音。

第一次操作很快就遇到一个实际限制：完整口播稿太长，不能一次性粘贴进去。

于是我先按 Scene 和语义段把稿子拆开，而不是为了凑字符数随便从中间截断：

```text
完整口播稿
    ↓
按 Scene 拆分
    ↓
在自然停顿或语义转折处继续拆分
    ↓
每段控制在约 300～450 字符
    ↓
分别生成文字朗读
    ↓
按原顺序放回同一条时间线
```

那一次我一共拆成了 8 个比较大的朗读片段。

切分时，我尽量不把一句话从中间截开，也不把每段贴到工具允许的字符上限，给字符统计误差留一点空间。这样做的好处是很快能得到一条可以听的样片，先判断口播语气、速度和整体长度是否合适。

但它也暴露了一个问题：我知道整段声音在哪里开始，却不知道其中某一句到底从什么时候开始。

比如一段音频从第 120 秒开始，持续 18 秒。里面可能有四句话，但我只有整段的起止时间：

```text
120.000s ───────────────────── 138.000s
        句 1？  句 2？  句 3？  句 4？
```

如果后面只是把整段声音当背景播放，这样勉强够用；但如果要让字幕逐句出现，让文件树、Diff 和测试状态跟着口播进入，就远远不够。

> 💡 **一句话总结**：大段 TTS 适合快速试声音，却不适合作为精确时间轴。

## 04 一整条 MP3，为什么不够用

如果我只保留一条完整音频，后面每次修改都会变得麻烦：

|遇到的变化|整条 MP3 的问题|
|---|---|
|只改一句话|可能需要重新生成整条声音|
|调整某个技术词|无法快速定位对应位置|
|生成字幕|只有粗粒度的时间码|
|同步 Scene|Scene 和声音之间没有稳定关系|
|替换一段语音|很难单独剪出并保持准确位置|

这让我意识到，TTS 的基本单位不能是「整条视频」，也不能只是「一大段文字」，而应该是：

```text
一个 Scene 里的一个 Narration Segment
```

这个改变看起来只是把文字切得更细，实际上会影响后面所有事情：

- 音频文件怎么命名。
- 字幕怎么生成。
- 画面动作从哪里找时间。
- 修改一句口播时能不能只替换这一句。
- Scene 的真实时长应该由什么决定。

我后来把这件事类比成给一整条铁路划分站点。整条 MP3 是从起点到终点的路线，但 Segment 是沿途有名字的车站。没有车站，你知道列车在运行，却不知道它现在经过哪一段；有了车站，声音、字幕和画面才有可能在同一个位置会合。

> 💡 **一句话总结**：要让声音驱动画面，时间轴必须有可以定位的中间单位。

## 05 我把口播稿整理成 TTS Script

确定要按 Segment 生成以后，我在独立的 TTS 项目里增加了一层 `TTS Script`。

它不是重新写一篇稿子，而是把已经确认的口播稿整理成机器可以逐段处理的结构。

结构大致是：

```json
{
  "videoId": "claude-code-video",
  "scenes": [
    {
      "sceneId": "06",
      "segments": [
        {
          "id": "06-01",
          "text": "这里其实还涉及一个现在非常重要的概念：Agent。"
        }
      ]
    }
  ]
}
```

### 我先把每个 Scene 拆成多个 Segment

比如 Scene 06，我没有再让它对应一整段长文字，而是拆成：

```text
06-01  引出 Agent
06-02  说明过去的人机工作方式
06-03  解释 Agent 的变化
06-04  给出新的分工结论
```

我尽量让一个 Segment 对应一句完整口播，或者一个不适合再拆开的短语。这样每段音频都比较短，意思也比较完整，后面要让某个画面动作跟着一句话出现时，可以直接找到对应时间点。

但 Segment 也不能切得过碎。一个词单独生成一段，虽然时间很细，声音的自然停顿却会被破坏；一段塞进太多意思，又会回到前面“大 MP3 不好定位”的问题。

所以我的判断标准不是字符数量本身，而是：这段话是否完成了一个独立的语义动作。

### 我再清理一次适合朗读的文本

真正生成前，我还会把 TTS 文本再过一遍：

- 清掉 Markdown 标题和格式符号。
- 删除不需要读出来的制作说明。
- 调整不自然的标点和多余空格。
- 确认数字的读法。
- 确认英文产品名和技术词的写法。
- 在需要停顿的地方补上合适的标点。

这一步看起来只是文字清理，但会直接改变语速、停顿和字幕内容。

我的做法是让 TTS Script 里的句子尽量接近最后实际听到、也实际显示出来的句子。否则，声音读的是一套文字，字幕显示的是另一套文字，后面再准确的时间也只能掩盖内容不一致。

> 💡 **一句话总结**：TTS Script 不是中间草稿，而是声音、字幕和时间轴共同使用的文字源。

## 06 我给每个 Segment 编一个固定 ID

我把 Segment ID 设计成 `Scene-序号` 的形式，例如：

```text
03-02
```

这个 ID 从 TTS Script 开始，一直保留到后面的文件和时间资料里：

```text
TTS Script
    ↓
音频文件
    ↓
Word Boundary
    ↓
Subtitle Data
    ↓
Audio Manifest
    ↓
Timeline Manifest
    ↓
Remotion 时间轴
```

我后来觉得，这个 ID 就像每句话的身份证。

以前如果只看文件顺序或整条 SRT，改过一句话以后，很容易不知道它原来属于哪一幕。固定 ID 让这句话在整个制作链路里一直能被找到：

```text
06-03
├── 06-03.mp3
├── 06-03.json
└── 字幕和画面动作的引用
```

它还有一个很实际的好处：如果某个技术词发音不对，我可以定位到 `06-03`，重新生成这一段，再更新对应的时间资料，不需要从头处理整支视频。

> 💡 **一句话总结**：固定 ID 让一句口播在整个生产流程里保持同一个身份。

## 07 我把分段生成搬到独立 TTS 项目

确定 Segment 结构以后，我没有继续在视频项目里手工生成声音，而是把 TTS 单独放到 `tts` 项目里处理。视频项目只接收已经整理好的结果。

我使用 `edge-tts` 按 Segment 批量生成中文语音。每个 Segment 生成一份独立 MP3，同时保存对应的 Word Boundary 时间信息。

文件按 Scene 组织：

```text
video-assets/
└── audio/
    ├── scene-01/
    │   ├── 01-01.mp3
    │   └── 01-02.mp3
    ├── scene-02/
    │   ├── 02-01.mp3
    │   └── 02-02.mp3
    └── ...
```

这样改完以后，如果某一段声音不满意，我只需要重新生成对应的 Segment，不会影响其他 Scene。某个技术词发音不对、语速需要调整时，也能直接定位到具体文件。

这时视频项目和 TTS 项目的边界也变得清楚了：

|项目|负责什么|
|---|---|
|TTS 项目|整理朗读文本、生成声音、记录语音边界和真实时长|
|视频项目|读取音频资料、组织画面、同步字幕和渲染视频|

这样做的原因不是把项目拆得越多越专业，而是让每个项目只负责自己最清楚的一段问题。TTS 不需要知道画面怎么动画，Remotion 也不需要重新猜一句话的声音长度。

> 💡 **一句话总结**：声音生成和视频渲染可以分开，但它们必须共享稳定的 Segment ID 和时间资料。

## 08 我没有只保存 MP3，还保存了 Word Boundary

生成音频时，我让 TTS 引擎同时返回每个词的边界信息，包括词或短语在当前 Segment 中的开始时间和持续时间。

我把它理解成每个 Segment 自己的一条小时间轴：

```text
06-01
├── 音频：06-01.mp3
└── 时间信息：06-01.json
    ├── Agent：开始时间
    ├── 概念：开始时间
    └── 变化：开始时间
```

有了这份信息，我就不用凭感觉把一句话平均分到几秒里，而是可以根据真实语音去生成字幕。

当然，Word Boundary 也不是让所有问题自动消失。它记录的是 TTS 引擎实际朗读时的边界，后面仍然要检查：

- 词和文字是否对应。
- 一条字幕是否适合在画面上阅读。
- 词级边界合并成短语后是否自然。
- 时间是否落在对应 Segment 的范围内。

它的价值是提供真实声音的时间依据，而不是替人完成所有字幕编辑。

> 💡 **一句话总结**：Word Boundary 是字幕和语音之间的刻度尺。

## 09 我用真实音频时长生成 Audio Manifest

每段 MP3 生成以后，我再读取它的真实时长，生成 `audio-manifest.json`。

我让这份 Manifest 记录：

- Segment ID。
- 文件路径。
- 对应文本。
- 实际音频时长。
- 每个 Scene 的旁白总时长。

示意结构是：

```json
{
  "sceneId": "03",
  "segments": [
    {
      "id": "03-01",
      "file": "audio/scene-03/03-01.mp3",
      "duration": 4.82
    }
  ],
  "narrationDuration": 4.82
}
```

从这里开始，我不再提前假设某个 Scene 一定是 10 秒或 20 秒，而是让真实语音时长先成为时间基础。后面的画面时长、音频起点和字幕时间，都围绕这份数据组织。

这和“先定一个固定视频总时长，再把文字硬塞进去”是两种完全不同的做法：

```text
错误方向：固定 Scene 时长 → 压缩或拉长口播
正确方向：确定口播内容 → 生成真实音频 → 用真实时长组织画面
```

如果一句话自然读完需要 4.82 秒，我不会为了凑整把它强行压成 4 秒，也不会只为了填满画面把静止状态拖到 8 秒。时间应该由内容和实际声音共同决定。

> 💡 **一句话总结**：真实音频时长是视频时间轴的起点，不是最后才补上的数据。

## 10 我根据 Word Boundary 生成逐句字幕

我没有直接拿原文章，也没有把一份粗粒度 SRT 当成最终字幕，而是按下面的关系生成：

```text
TTS Script
    ↓
每个 Segment 的文字
    ↓
Word Boundary
    ↓
Subtitle Cue
```

字幕的关系保持在：

```text
Scene
└── Narration Segment
    └── Subtitle Cue
```

例如一个 Segment `03-02`，可以继续产生：

```text
03-02-a
03-02-b
03-02-c
```

我生成以后会检查字幕是不是完整句子或自然短语，单条字幕会不会太长，能不能在一到两行内读完，以及它的时间有没有落在对应 Segment 里。

这里我没有追求“每个词都单独变成一条字幕”。Word Boundary 是更细的底层时间资料，屏幕字幕仍然要按阅读节奏合并。声音可以很细，画面上的文字反而需要克制。

> 💡 **一句话总结**：底层时间可以精细，最终字幕要服从观众的阅读速度。

## 11 我把所有东西放进同一条 Timeline

当每个 Segment 都有真实音频时长以后，我再按照 Scene 和 Segment 的顺序累加时间：

```text
Scene 01
  01-01  offset 0.000
  01-02  offset 4.176
  01-03  offset 7.320

Scene 02
  02-01  offset 21.576
  02-02  offset 27.288
```

这份时间线记录：

- 每个 Scene 的开始时间。
- 每个 Segment 的开始偏移和结束时间。
- 对应的音频文件。
- 对应的字幕文件或 Cue。
- 后续画面动作可以引用的语义锚点。

时间关系大致是：

```text
Scene 全局偏移
    + Segment 在 Scene 内的偏移
    = Segment 在整条视频中的开始时间
```

这条视频最后生成了 14 个 Scene、166 个语音 Segment 和 269 条字幕 Cue，旁白总时长约为 642.744 秒。

这些数量本身不是最重要的。重要的是，Scene、音频和字幕都来自同一套 Segment 结构，不需要我再分别维护三张互相独立的时间表。

如果后面我要让某个动作跟着 `08-04` 出现，Remotion 不需要猜它应该位于第几秒，只需要读取这个 Segment 在 Timeline 中的时间。

> 💡 **一句话总结**：统一时间线的作用，是让声音、字幕和画面都对同一份时间资料负责。

## 12 五个最容易出现的问题

### 我没有再把整篇稿子一次生成

整篇生成看起来最省事，但后面重试、定位和调速都不方便。

所以我最后固定成：

```text
Scene → Segment → 单段音频
```

### 我没有只留下最终合并音频

完整音频适合作为交付文件，但不适合作为内部时间轴的唯一来源。

我把每段 MP3、对应的边界信息和 Manifest 都保留下来，之后需要替换时才有抓手。

### 我没有事后再拆长字幕

如果一开始只有一份长段 SRT，后面再手工拆成很多句，时间误差会从前往后累积。

我的做法是从逐 Segment 的文字和 Word Boundary 开始生成字幕，让字幕从源头就带着边界。

### 我没有把 Segment 切得越细越好

一个词单独生成一段，虽然时间很细，但容易破坏声音的自然停顿；一段塞进太多意思，又会回到无法定位的问题。

我最后采用的标准是：一个 Segment 对应一句完整口播，或者一个不适合再拆开的短语。

### 我没有因为画面 Scene 变化就重新生成音频

如果只是重新调整画面切分，但口播文字和顺序没变，原来的 Segment 音频可以继续使用。

只有改了口播文字、增删句子、改变顺序，或者修改技术词发音时，我才需要重新生成对应音频。

> 💡 **一句话总结**：Segment 是声音的复用单位，不能只把它当成一次性的切片。

## 13 动手做出一套可同步的 TTS 资料

如果你也要把一篇视频口播稿交给 TTS，我现在会按下面的顺序处理。

先准备已经确认过的口播内容：

```text
video/
├── narration-script.md
└── tts-script.json
```

然后要求工具先检查环境和输入，不要直接生成最终声音：

```text
请把当前视频口播稿整理成适合 TTS 的分段脚本。

要求：
1. 以 Scene 为一级结构，以语义完整的 Narration Segment 为二级结构。
2. 每个 Segment 使用固定 ID，格式为 Scene-序号，例如 06-03。
3. 清理 Markdown 标题、格式符号和制作备注，但不要改变口播含义。
4. 检查产品名、技术词、数字和英文的朗读方式。
5. 不要为了凑字符数从句子中间截断。
6. 先输出 TTS Script 和分段说明，不要直接生成整条合并音频。
7. 生成时保留每个 Segment 的独立音频和 Word Boundary。
8. 后续输出 Audio Manifest、Subtitle Manifest 和 Timeline Manifest。
```

工具生成后，我会先试听几个代表性 Segment：开场、概念解释、技术词较多的一段和总结。声音、语速和停顿没有问题，再批量生成全部 Segment。

最后检查四件事：

1. 每个 Segment ID 是否唯一，并且和 Scene 对应。
2. 每个 MP3 是否有真实时长。
3. 每个 Segment 是否有对应的 Word Boundary。
4. 字幕 Cue 和 Timeline 是否都能回溯到同一个 Segment。

最后应该拿到的不是单个文件，而是一组相互关联的资料：

```text
video-assets/
├── tts-script.json
├── audio/
│   ├── scene-01/01-01.mp3
│   └── ...
├── word-boundaries/
│   ├── 01-01.json
│   └── ...
├── audio-manifest.json
├── subtitle-manifest.json
├── timeline-manifest.json
├── narration.srt
└── narration.vtt
```

以后如果只想换掉一句口播，就找到对应的 Segment，重新生成它的 MP3 和时间资料，再让视频项目重新读取 Manifest。整条视频不需要从头猜时间。

> 💡 **一句话总结**：先按语义拆分，再生成声音；先保存中间时间资料，再合并交付文件。

## 小结

这一篇，我没有把 TTS 理解成一个“输入文字、输出 MP3”的黑盒，而是把它放进完整的视频生产流程里重新看了一遍。

真正值得带走的是三件事：

1. 口播稿、TTS Script 和最终字幕不是同一份资料，但它们应该来自同一套语义结构。
2. `Scene → Segment → Audio` 是比整条 MP3 更适合视频制作的组织方式。
3. Word Boundary、真实音频时长和 Timeline Manifest，决定了后面的字幕和画面能不能准确同步。

这次最后生成了 14 个 Scene、166 段语音和 269 条字幕 Cue。对我来说，真正重要的不是这些数字，而是每一句口播都拥有自己的 ID、声音和时间范围。

我最后把这一阶段总结成一句话：

> TTS 不是把文字读出来，而是把每段口播变成后续画面可以准确引用的时间资料。

上一篇先把无声画面做成立，下一篇就要把这些真实声音、字幕时间和画面动作接回 Remotion，看看它们第一次合在一起时，哪些地方会真正同步，哪些地方还会提前或滞后。

## 参考资料

- [TTS 项目设计与实现说明](../tts/独立%20TTS%20项目设计与实现说明.md)
- [TTS 口播稿](../tts/projects/what-is-claude-code/source/narration-script.md)
- [TTS Script](../tts/projects/what-is-claude-code/video-assets/tts-script.json)
- [Audio Manifest](../tts/projects/what-is-claude-code/video-assets/audio-manifest.json)
- [Timeline Manifest](../tts/projects/what-is-claude-code/video-assets/timeline-manifest.json)
- [视频制作问题与经验](docs/video-production-notes.md)
- [第二部分：从视觉方向到 TTS 之前](video-production-process-part-2.md)
