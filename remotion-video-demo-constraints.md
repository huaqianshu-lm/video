---
title: Remotion 视频 Demo 项目约束
date: 2026-07-22
tags:
  - Remotion
  - Claude Code
  - Video Demo
  - Project Constraints
source: 基于 notes/2026-07-22-claude-code-remotion-video.md 整理
---

# Remotion 视频 Demo 项目约束

这份文档用于指导 Claude Code 实现一个最小 Remotion 视频 demo。目标不是做完整视频产品，也不是验证复杂剪辑能力，而是跑通“自然语言需求 → Remotion 代码 → 预览 → 修改 → 渲染 mp4”的最小链路。

## 1. 项目目标

实现一个可预览、可渲染的 Remotion 视频 demo，用来验证 Claude Code + Remotion 是否适合后续内容生产。

成功标准：

- 能启动 Remotion Studio 预览。
- 能看到一个 6-10 秒的简单动画视频。
- 能通过自然语言修改一个明确细节，并在预览中看到变化。
- 能最终渲染出一个 mp4 文件。
- 代码结构简单，后续可以作为模板继续改。

## 2. 适用场景边界

这个 demo 只验证 Remotion 擅长的内容：

- 文字动画
- Logo / 标题片头
- 知识卡片动画
- 简单数据或数字变化
- 产品 / 工具发布预告式短视频
- 可复用、可参数化的视频模板

不要在这个 demo 中做：

- 真人实拍剪辑
- 旅行 vlog / 婚礼视频 / 口播素材剪辑
- 一次性创意 MV
- 多段复杂转场长视频
- 音乐节奏强绑定的视频
- 大量素材管理和剪辑

判断标准：这个 demo 应该是“能换文字、颜色、数据后复用的模板”，不是一次性艺术作品。

## 3. Demo 内容建议

优先做一个最小知识卡片片头，避免一开始做复杂项目。

建议规格：

- 时长：6-10 秒
- 尺寸：1920 × 1080
- 帧率：30fps
- 背景：深色背景，可用纯黑或深色渐变
- 主体：一个标题、一句副标题、一个简单高亮元素
- 动画：淡入、轻微放大、弹性进入、简单发光即可
- 输出：`out/demo.mp4`

建议文案：

- 标题：`Memora`
- 副标题：`让记忆流光化为洞察与输出`
- 辅助文字：`AI Knowledge Workflow`

## 4. 技术约束

- 使用 Remotion。
- 使用 React / TypeScript。
- Node.js 版本必须为 18 或更高。
- 项目应能通过 `npm install` 安装依赖。
- 项目应能通过 `npx remotion studio` 启动预览。
- 项目应能通过 `npx remotion render <CompositionId> out/demo.mp4` 渲染视频。
- 只需要一个 Composition，不要拆多个复杂场景。
- 不要引入数据库、后端服务、登录系统、部署配置。
- 不要为了 demo 引入复杂状态管理。
- 不要添加与视频 demo 无关的功能。

## 5. 代码约束

- 代码优先简单清晰，不要过度抽象。
- 动画参数可以先硬编码，暂时不需要配置系统。
- 只保留 demo 必需文件。
- Composition ID 要明确，并在最终说明里写出来。
- 关键动画应优先使用 Remotion 的基础能力：
  - `useCurrentFrame()`：获取当前帧
  - `interpolate()`：做淡入、位移、缩放等平滑过渡
  - `spring()`：做弹性进入效果
- 如果使用延迟动画，注意避免传给 `spring()` 的帧数为负数。
- 不要一次性生成复杂多场景代码；先跑通最小版本。

## 6. 视觉约束

- 整体风格：干净、科技感、克制。
- 背景不要花哨，优先深色。
- 字体层级要清楚：标题最大，副标题次之，辅助文字最小。
- 动画不要过快，避免一闪而过。
- 发光效果要轻，不要刺眼。
- 画面元素不要太多，保持一个中心视觉。
- 不追求一次到位，先保证可读、可预览、可修改。

## 7. 工作流程约束

必须按这个顺序执行：

1. 检查 Node.js 版本：`node --version`。
2. 初始化 Remotion 项目和最小代码。
3. 安装依赖：`npm install`。
4. 启动预览：`npx remotion studio`。
5. 在预览中检查动画效果。
6. 根据反馈修改一个明确细节。
7. 再次预览确认。
8. 满意后再渲染：`npx remotion render <CompositionId> out/demo.mp4`。

重要规则：

- 预览阶段用来调效果。
- 渲染只在最后执行，不要每改一次就渲染一次。
- 如果 Composition ID 不确定，先查代码或 Remotion Studio，不要猜。
- 如果渲染阶段需要下载 Chromium 组件，网络卡住时应说明原因，不要瞎改代码。

## 8. 可接受的第一版效果

第一版只要满足以下效果即可：

- 标题从透明淡入。
- 标题轻微放大或弹性进入。
- 副标题稍后出现。
- 背景有轻微渐变或光晕。
- 最后停留 1-2 秒，方便看清文字。
- 能在 Remotion Studio 里流畅预览。

第一版不要追求：

- 多镜头叙事
- 复杂转场
- 音乐同步
- 真实素材剪辑
- 自动批量生成多个视频
- 复杂配置化模板

## 9. 验证清单

完成后必须确认：

- [ ] `node --version` 显示 Node.js 18+。
- [ ] `npm install` 成功。
- [ ] `npx remotion studio` 能启动。
- [ ] 预览中能看到完整动画。
- [ ] 至少根据一句自然语言反馈修改过一个细节。
- [ ] 修改后预览能看到变化。
- [ ] `npx remotion render <CompositionId> out/demo.mp4` 成功。
- [ ] `out/demo.mp4` 文件存在。
- [ ] 最终说明里写清楚 Composition ID、预览命令、渲染命令。

## 10. 给 Claude Code 的起始提示词

可以直接把下面这段发给 Claude Code：

```text
请基于当前目录实现一个最小 Remotion 视频 demo。

目标：做一个 6-10 秒的知识卡片片头视频，用来验证 Claude Code + Remotion 的效果。

规格：
- 1920x1080
- 30fps
- 深色背景
- 标题：Memora
- 副标题：让记忆流光化为洞察与输出
- 辅助文字：AI Knowledge Workflow
- 标题从透明淡入，并轻微弹性放大
- 副标题稍后淡入
- 最后停留 1-2 秒
- 只要一个 Composition
- 输出文件路径：out/demo.mp4

约束：
- 使用 Remotion + React + TypeScript
- 代码保持简单，不要过度抽象
- 不要做真人素材剪辑
- 不要做复杂多场景
- 不要添加后端、数据库、登录、部署等无关功能
- 先保证能通过 npx remotion studio 预览
- 预览确认后再渲染 mp4

请先检查 Node.js 版本是否 >=18，然后搭建项目、写最小代码、告诉我预览命令和 Composition ID。渲染前先让我确认预览效果。
```

## 11. 后续扩展方向

只有当这个 demo 跑通并且确实有复用价值时，再考虑扩展：

- 把标题、副标题、颜色抽成参数。
- 增加一个简单数据动画版本。
- 增加竖屏版本：1080 × 1920。
- 接入知识库 brief，把文章摘要转成视频卡片。
- 与 TTS 配音或其他视频剪辑工具组合。

不要在第一版就做这些扩展。第一版只验证最小链路。