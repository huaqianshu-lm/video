# 我是怎么做视频的（四）：一接入 TTS，为什么画面和声音又对不上了

上一篇，我把口播稿拆成了 Segment，并为每个 Segment 生成了 MP3、Word Boundary、字幕数据和统一时间资料。

这一次，我要把这些资料真正接回 Remotion。

我原本以为，接入过程就是把音频文件放进 `Sequence`，再把字幕显示出来。真正进入 Studio 以后，我才发现，声音、字幕和画面其实各自有一套时间。如果不把它们收回到同一个时间轴里，视频虽然能播放，但每一层都可能在不同的时刻发生：声音已经说到下一句，字幕还停在上一句，画面动作又早就完成了。

这一篇记录我怎样处理这几类问题：

- TTS 项目输出的时间资料，怎样接进 Remotion。
- Scene、Segment、Audio 和 Subtitle Cue，怎样共用同一套时间关系。
- 为什么长 Scene 里的动画会在旁白结束前提前完成。
- 为什么一份粗粒度 SRT 从第二个 Scene 开始逐渐落后。
- 为什么短音频开头像是丢了几个字，以及我最后怎样定位它。

渲染和最终交付放到下一篇。这一篇先停在 Studio 里的音画同步。

**看完这一篇，你会拿到：**

- 一套把 TTS Manifest 接回 Remotion 的流程。
- 一种用真实音频时间计算 Scene 和字幕时间的方法。
- 一种让主要画面动作跟随 Segment 语义发生的方法。
- 一套排查字幕错位、动画提前结束和短音频丢字的顺序。

> 💡 **一句话总结**：接入 TTS 不是把声音放进视频，而是让声音成为整条视频的时间轴基础。

## 01 我先确认 TTS 到底交付了什么

上一篇结束时，我手里已经不只有 MP3。

TTS 项目实际交付给视频项目的是三类时间资料：

```
Scene Offset
Segment Offset
Subtitle Cue
```

它们分别回答三个问题：

```
Scene Offset
→ 这一幕从整条视频的什么时候开始

Segment Offset
→ 这一句口播从当前 Scene 的什么时候开始

Subtitle Cue
→ 这一句字幕在什么时候出现和结束
```

我没有在视频项目里重新测量音频，也没有把总 SRT 再手工拆一遍。我的做法是直接把 TTS 项目的 Manifest 当成视频项目的上游资料：

> TTS 负责产生真实时间，Remotion 负责消费这些时间并安排画面。

这件事很像拍摄时的场记。演员说了多久、某一句从哪一秒开始，应该由录音和场记资料确定；剪辑软件只负责把这些结果放到正确的位置。如果我在 Remotion 里再凭感觉猜一次，前面已经建立好的 Segment 关系就被破坏了。

> 💡 **一句话总结**：上游已经生成真实时间，视频项目应该消费时间，而不是重新猜时间。

## 02 我先把音频放到 Remotion 能读取的位置

TTS 项目里的音频文件，并不能直接被 Remotion 的浏览器预览读取。所以我先把需要播放的资源复制到视频项目的静态资源目录：

```
TTS 项目
└── video-assets/audio/
        ↓
Video 项目
└── public/local-assets/claude-code-what-is-v2/
```

复制以后，文件仍然按 Scene 和 Segment 组织，ID 不重新命名：

```
audio/scene-06/06-01.mp3
audio/scene-06/06-02.mp3
```

我先做了一次资源核对：

- 音频数量是否完整。
- Scene 目录是否对应。
- 文件名是否和 Segment ID 一致。
- Manifest 里的路径能不能找到实际文件。

这一步只解决「Remotion 能不能读到音频」，不改变音频内容，也不重新合并文件。

如果这一步不先做，后面在 Studio 里听到没有声音，我就分不清到底是时间轴错了，还是文件根本没有被读到。把资源问题和时间问题分开，是我后面排查同步问题的第一个习惯。

> 💡 **一句话总结**：先确认每个音频文件真的存在，再开始讨论它应该什么时候播放。

## 03 我把 Manifest 翻译成 30fps 时间线

拿到 Manifest 以后，我需要把其中的秒数转换成 Remotion 使用的帧。

我需要从它生成四类结果：

|时间资料|在 Remotion 中负责什么|
|---|---|
|Scene Offset|确定每个 Scene 在整条视频中的位置|
|Segment Offset|确定每段 MP3 的播放起点|
|Segment Duration|确定音频需要占用多少帧|
|Subtitle Cue|确定字幕的显示起止时间|

Scene 和 Segment 的关系是：

```
Scene 全局偏移
+ Segment 在 Scene 内的偏移
= Segment 在整条视频中的开始时间
```

比如 Scene 02 从整条视频的 21.576 秒开始，Segment 02-03 在 Scene 02 的 8.520 秒开始，那么它在整条视频中的起点就是：

```
21.576 + 8.520 = 30.096 秒
```

最后再按 30fps 换算成帧。

这里有一个容易被忽略的问题：Scene、Audio 和 Caption 不能各自计算、各自取整。假设 Scene 在一套边界上取整，音频在另一套边界上取整，字幕又直接使用浮点秒数，前面的零点几帧就可能在后面逐渐累积成明显错位。

所以我尽量让三者使用同一套帧边界。时间线不是三份数据拼在一起，而是同一份时间关系的三个使用方式。

> 💡 **一句话总结**：所有秒数都要落到同一套 30fps 帧边界上，避免误差分别累计。

## 04 我没有一看到不同步就先改画面

第一次接入时，我把检查拆成三层。

### 第一层：我先确认声音能播放

我先检查每个 Segment 的 MP3 路径、顺序和时长，确认不是文件缺失或音频错位。

### 第二层：我再确认字幕覆盖声音

然后我检查字幕 Cue 是否属于正确的 Segment，有没有空档、重叠，或者超出当前 Segment 实际时长的情况。

### 第三层：我最后看画面动作有没有跟上语义

最后才进入画面层，对照口播检查：

```
说到「读取文件」→ 文件树开始扫描
说到「修改代码」→ Diff 面板出现
说到「运行测试」→ Terminal 开始输出
说到「人工确认」→ 画面停在 Review 状态
```

这样分层以后，问题就有了比较明确的归属：

```
没有声音
→ 查资源路径和音频加载

字幕和声音错位
→ 查 Subtitle Manifest 和帧转换

声音、字幕都正确，但画面提前完成
→ 查 Scene 内部动画和 Segment 映射
```

如果音频还没有加载成功，我不会先调整画面；如果字幕时间不对，我也不会马上认定 Scene 时长有问题。先判断是哪一层错了，才能知道应该改源文件、Manifest、帧转换还是组件动画。

> 💡 **一句话总结**：音画同步问题要分层定位，不能把所有不同步都归因到画面代码。

## 05 我第一次接入后发现：长 Scene 的动画太早结束

TTS 接进来以后，有些 Scene 被真实旁白拉长了，但原来静音样片里的动画仍然使用早期的固定帧偏移。

我在 Studio 里看到的结果是：

```
场景开始
→ 几秒内列表和终端内容全部出现
→ 后面的旁白还在继续
→ 画面长时间停在最终状态
```

我一开始以为是音频时间线出了问题。后来对照配置才发现，声音的时长已经变了，画面却还把每个动作当成一个很短的 Scene 来处理。

这就像一个人用正常速度讲完一段很长的解释，但 PPT 在前两秒就把所有内容全部翻完了。声音没有错，画面也没有完全消失，只是两者的节奏不再属于同一条时间线。

所以我先把画面节奏改成相对于 Scene 时长分布：

```
Scene 开始
→ 标题和第一层状态进入
→ 主要内容在中段展开
→ 最终状态靠近结尾形成
→ 结尾保留阅读和思考时间
```

这一层调整解决的是 Scene 级别的节奏问题：画面不会在旁白刚开始没多久就全部完成。

但它还不能保证某个具体动作一定出现在正确的那句话上。

> 💡 **一句话总结**：真实音频拉长 Scene 后，静音样片里的固定帧动画也必须重新获得时间意识。

## 06 我再把关键动作绑定到 Segment

只按 Scene 总时长分布还不够。

比如画面应该在口播说到「读取文件」时扫描文件树，这个动作就应该跟着对应的 Segment 发生，而不是永远固定在场景开始后的第几秒。

于是我把主要视觉事件绑定到 Segment ID：

```
Segment 07-03 出现
→ 文件树扫描

Segment 08-04 出现
→ Diff 面板打开

Segment 08-06 出现
→ 测试命令执行
```

这样前面某句话的语速发生变化时，动作也会跟着真实语义时间移动。

我保留固定帧动画的地方，只用来做同一个 Segment 内部的微小过渡，例如淡入、轻微位移或状态高亮。跨句子的主要视觉事件，不再依赖脱离口播的固定帧。

我后来把这条边界记成：

```
跨 Segment 的主要事件
→ 绑定 Segment 时间

同一 Segment 内的微动画
→ 使用少量相对帧偏移
```

这让画面既能跟随语义，又不会把每个淡入效果都变成一条复杂的时间配置。

> 💡 **一句话总结**：Scene 决定一幕的范围，Segment 决定这一幕里的关键动作什么时候发生。

## 07 我第一次遇到的字幕问题：粗粒度 SRT 会逐渐落后

早期接入时，我曾经直接使用一份总 SRT。

刚开始播放时，看起来没有明显问题；但从第二个 Scene 往后，字幕开始慢慢落后于音频。越往后，错位越明显。

我后来对照文件发现，问题不是字幕文字写错了，而是这份 SRT 只有少量长段时间码，和实际已经拆成很多句的口播不是同一粒度：

```
长段 SRT
≠
逐句口播字幕
```

如果我再把一条长字幕平均拆成很多句，得到的只是看起来更细的时间码，不是真实的句子时间。前面的误差会继续累积到后面，文字虽然对，时间却不会真正准确。

所以我改用上一篇已经生成的逐句资料：

```
TTS Segment
→ Word Boundary
→ Subtitle Cue
```

每个 Segment 的 Word Boundary 记录了其中词语的真实出现时间，我再根据这些边界生成可读的 Subtitle Cue。总 SRT 和 VTT 仍然保留，方便交付和人工检查，但不再把它们当成视频内部时间轴的唯一来源。

> 💡 **一句话总结**：字幕要和音频保持同一粒度，不能用粗粒度时间码假装精确同步。

## 08 我把 Scene、Audio 和 Caption 收回同一棵树

如果三个部分各自维护时间，我实际上会同时拥有三张可能互相漂移的时间表：

```
Scene 自己的起止时间
音频真实语句时间
字幕手工拆出的时间
```

我的调整方式是把它们收回到一棵关系里：

```
Scene
└── Segment
    ├── Audio
    └── Subtitle Cue
```

这棵树不是为了让数据看起来整齐，而是为了明确时间的来源：

- Scene Offset 决定这一幕在整条视频中的位置。
- Segment Offset 决定这一句声音从哪里开始。
- Audio Duration 决定声音实际占多长时间。
- Subtitle Cue 决定这一句字幕何时显示。
- Segment ID 决定画面动作应该跟随哪一句口播。

接入以后，我先检查数量和 ID 是否完整：当前这条视频是 14 个 Scene、166 段音频和 269 条字幕 Cue。数量对上以后，再进入 Studio 看真实播放效果。

这个顺序很重要。数据结构没有对齐时，直接看画面只会得到「好像不对」的感觉；先确认关系完整，再看播放，才能把视觉问题和数据问题分开。

> 💡 **一句话总结**：同步不是把三条时间线调到差不多，而是让三者从同一棵数据结构中计算出来。

## 09 我第二次遇到的问题：音频开头像是丢了几个字

统一时间线接入以后，我在 Studio 里又听到一个现象：有些很短的音频像是少了开头一两个词。

这次我没有直接重新生成全部 TTS，而是按下面的顺序检查：

1. Word Boundary 里有没有首词。
2. MP3 源文件里是不是真的缺少首词。
3. 音频有没有设置淡入或其他音量变化。
4. 音频是不是刚好到开始帧才第一次加载。

最后我确认，源文件里首词存在，Word Boundary 也有首词记录，前置静音很短，代码中没有淡入。真正的风险来自很多短 MP3 都在开始播放的那一帧才挂载，Studio 连续播放时可能还没有完成加载和解码。

这时如果我直接重新生成 TTS，就会把一个播放器加载问题误判成音频生成问题，既浪费时间，也会破坏已经确认过的声音资料。

> 💡 **一句话总结**：听到音频开头丢字时，先证明源文件真的有问题，再处理播放器的加载时机。

## 10 我给短音频增加预挂载和缓冲

最后我在 Remotion 里做了两点调整：

- 每个音频 `Sequence` 提前约 2 秒挂载。
- 播放前等待媒体完成必要的缓冲。

代码层面的关系是：

```
Segment 的真实起点
        ↑
提前 2 秒预挂载和准备音频
        ↓
到达起点时开始播放
```

预挂载不会改变音频的实际起点，也不会把声音提前播放。它只是在真正的播放时刻之前，给浏览器留下加载和解码的准备时间。

这次排查让我留下了一个很具体的顺序：

```
Word Boundary 首词
→ MP3 前置静音
→ volume／fade
→ 媒体预挂载与缓冲
```

以后再遇到类似问题，我会先沿这条链路排查，而不是第一反应就重新生成全部音频。

> 💡 **一句话总结**：短音频连续播放时，播放起点和媒体准备时间是两件事。

## 11 我没有把所有画面动作都绑到音频起点

把关键动作绑定到 Segment，并不意味着每一个动画属性都要直接使用音频起点。

如果一个卡片从透明变成完全可见需要约半秒，而我把动画起点设在口播关键词刚出现的那一帧，观众仍然可能感觉画面慢了半拍。

所以我把画面动作分成两类：

|动作类型|时间处理方式|
|---|---|
|主要视觉事件|绑定对应 Segment 的语义起点|
|事件内部的淡入、位移和高亮|在 Segment 起点附近使用相对帧|

这条边界让我可以同时保证两件事：主要画面不会脱离口播，微动画又可以提前一点开始，让关键词出现时画面已经基本可见。

因此，视觉同步不是机械地让所有动画都和声音同一帧开始，而是让观众感知到的画面事件和口播语义同时成立。

> 💡 **一句话总结**：时间轴对齐的是观众感知到的主要事件，微动画可以根据入场耗时做小幅提前。

## 12 我在 Studio 里做最后的三轮检查

完成接入以后，我按固定顺序人工预览，而不是从头到尾只看一遍。

### 第一轮：我先听声音

我检查每个 Scene 的顺序，Segment 之间有没有重复、缺失或突然跳转，也特别注意短音频的第一个词是否完整。

### 第二轮：我再看字幕

我检查字幕是否跟着实际语句出现，有没有明显提前、延迟、空档、重叠或被画面遮挡。

### 第三轮：我最后看语义动作

我不只看画面「有没有动」，还看动作是不是出现在正确的那句话上：

```
口播语义
→ 对应字幕
→ 对应画面动作
```

我会重点抽查这些节点：

```
读取文件
→ 文件树开始扫描

修改代码
→ Diff 面板出现

运行测试
→ Terminal 开始输出

人工确认
→ 画面停在 Review 状态
```

如果声音和字幕都对，但画面已经提前完成，问题通常在 Scene 内部动画；如果三者一起错位，我就回头检查 Manifest、Scene Offset、Segment Offset 和帧转换。

> 💡 **一句话总结**：Studio 预览要检查语义关系，不只是检查声音、字幕和画面是否都存在。

## 13 我最后形成了一条可复用的接入流程

这次接入以后，我把流程收成这样：

```
读取 TTS Manifest
→ 复制音频资源
→ 校验 Segment ID 和文件路径
→ 统一换算 30fps 帧
→ 生成 Scene／Audio／Caption 时间线
→ 关闭旧的估算字幕
→ 把关键动作绑定到 Segment
→ 调整长 Scene 的动画节奏
→ 检查音频加载和预挂载
→ Studio 逐幕人工预览
```

我也整理了一段以后可以直接复用的执行提示词：

```
请把当前 TTS 项目的 Timeline Manifest、Audio Manifest 和 Subtitle Manifest 接入 Remotion。

要求：
1. Scene、Audio 和 Caption 使用同一套 30fps 帧边界。
2. 每个 Segment 保留原始 Segment ID，不重新按文件顺序命名。
3. 音频从 Manifest 的真实 Scene Offset 和 Segment Offset 计算播放起点。
4. Subtitle Cue 从逐句字幕 Manifest 生成，不使用粗粒度 SRT 反推内部时间轴。
5. 如果已有场景内估算字幕，接入真实字幕后关闭它，避免出现两层字幕。
6. 主要视觉事件绑定对应 Segment 的 Scene 局部起点。
7. 音频 Sequence 提前预挂载，并开启必要的播放缓冲。
8. 完成后先检查 Scene、Segment、Cue 数量、ID 和资源路径，再进入 Studio 检查音画同步。
```

这段提示词里最重要的不是具体 API，而是先把时间源、ID、字幕来源和验证顺序说清楚。否则即使代码能编译，也可能只是把三套时间资料同时放进了项目，并没有真正建立同步关系。

> 💡 **一句话总结**：可复用的不是某一段音频接入代码，而是「统一时间源、固定 ID、分层验证」这套顺序。

## 小结

这一篇我主要解决了三个问题：

1. 用 TTS Manifest 作为 Remotion 的统一时间源。
2. 用 Segment ID 把音频、字幕和主要画面动作联系起来。
3. 用分层排查处理动画提前结束、字幕逐渐落后和短音频开头丢字。

我最后形成的判断方式是：

```
先确认源文件
→ 再确认 Manifest 和资源路径
→ 再确认 30fps 帧边界
→ 再确认媒体加载
→ 最后调整画面动作
```

这一步完成以后，视频才真正从「有画面的静音样片」进入「有真实声音、字幕和语义节奏的视频」。下一篇，我再记录怎样在预览确认以后完成渲染，并检查最终交付的 MP4。

## 参考资料

- [TTS 制作全过程](video-production-process-part-3.md)
- [视频制作问题与经验](docs/video-production-notes.md)
- [当前视频时间线配置](src/videos/claude-code-what-is/video14.config.ts)
- [当前视频组件](src/videos/claude-code-what-is/ClaudeCodeWhatIsVideo14.tsx)
- [当前字幕组件](src/components/TimedCaption.tsx)
- [当前视频生产资料](videos/claude-code-what-is/)
