主人，结论是：

> 这个升级方案的总体方向可行，而且抓住了真正需要补的能力：状态管理、机器可读 Manifest、自动校验、人工 Gate 和失效传播。  
> 但按目前项目的真实情况，不能直接按文档描述一次性实现成“从源文档自动跑到最终 MP4”的完整流水线。第一版需要缩小自动化范围，先打通确定性的部分。

## 当前项目已经具备的能力

目前已经验证过：

- Remotion 横屏视频和多个 Composition。
- 音频驱动 Scene 时间轴。
- Segment 音频、字幕和 Timeline Manifest。
- 1 条视频的本地资源打包。
- GitHub Smoke Render。
- GitHub Full Render。
- Linux 中文字体安装和检查。

实际代码可以参考：

- [Root.tsx](/Users/limiao/personal/2-topic/4-AI/project/video/src/Root.tsx:1)
- [claude-code-install/video.config.ts](/Users/limiao/personal/2-topic/4-AI/project/video/src/videos/claude-code-install/video.config.ts:389)
- [audio-manifest.json](/Users/limiao/personal/2-topic/4-AI/project/video/src/videos/claude-code-install/generated/audio-manifest.json:1)
- [timeline-manifest.json](/Users/limiao/personal/2-topic/4-AI/project/video/src/videos/claude-code-install/generated/timeline-manifest.json:1)

所以，方案中的这条链路是可行的：

```text
TTS 结果
→ Manifest 校验
→ Remotion 时间轴
→ 本地检查
→ GitHub Smoke
→ GitHub Full Render
```

## 目前还没有的能力

升级方案中以下内容目前只是设计，没有实际实现：

- `production-state.json`。
- Pipeline Controller。
- `video pipeline start/status/approve/resume` 命令。
- Markdown 对应的机器可读生产资料。
- Video 到 TTS 的自动调用。
- TTS 结果自动回传。
- TTS 版本和 Narration Hash 校验。
- 自动失效传播。
- 通用 GitHub Actions。
- GitHub Run 自动触发。
- 每 20 分钟远程轮询。
- Artifact 自动下载和验证。
- 自动生成 Remotion Scene 实现。

当前项目也没有 `scripts/` 目录或流程控制入口，`package.json` 里的渲染命令仍然写死为：

```json
"render": "remotion render claude-code-install out/claude-code-install.mp4"
```

GitHub 工作流也仍然写死了：

- `claude-code-install`
- `assets/claude-code-install-assets.zip`
- 76 个 MP3
- 固定 Composition ID
- 固定 Artifact 名称

具体见：

- [smoke-test-video.yml](/Users/limiao/personal/2-topic/4-AI/project/video/.github/workflows/smoke-test-video.yml:31)
- [render-video.yml](/Users/limiao/personal/2-topic/4-AI/project/video/.github/workflows/render-video.yml:31)

## 方案中最需要修正的地方

### 1. “自动生成内容资料”不是普通程序可以直接完成的

方案写成：

```text
video pipeline start <slug>
→ 自动生成 Content Analysis
→ 自动生成 Narrative
→ 自动生成 Scene Script
```

这需要一个明确的内容生成引擎。目前项目中没有：

- OpenAI API 调用。
- Claude API 调用。
- 可编程调用的 Claude Code CLI 协议。
- 生成内容时使用的固定 Prompt 和返回格式。

因此，Pipeline Controller 可以自动执行文件检查、TTS、Manifest、Remotion 和 GitHub，但不能仅靠一个 TypeScript CLI 自动生成高质量内容资料。

第一版应区分两类自动化：

```text
创作型任务：
由 Claude Code／Agent 生成，人确认

确定性任务：
由 Pipeline Controller 自动执行
```

也就是说，方案可以支持“Agent 按状态继续工作”，但不能把它描述成一个完全独立、无需 Agent 的普通程序。

### 2. 四个 Gate 与当前项目规则存在冲突

升级方案把人工确认压缩成四个：

1. 内容方案。
2. 创作方案。
3. 完整视频预览。
4. 最终成片。

这在效率上是合理的，但当前 `CLAUDE.md` 规定的是：

- Content Analysis 后确认。
- Video Narrative 后确认。
- Scene Script 后确认。
- Narration Script 后确认。
- Visual Script 后确认。
- Visual Prototype 后确认。

所以四 Gate 方案不是直接兼容，而是一次流程规则调整。

我的建议是保留四个用户操作节点，但保留细分阶段和回退能力：

```text
Gate 1：
Content Analysis
+ Video Narrative
+ Scene Script

Gate 2：
Narration Script
+ Visual Script
+ Visual Prototype

Gate 3：
Remotion Studio 预览

Gate 4：
最终 MP4
```

这样用户不用每生成一个文件就单独确认，但如果 Gate 1 只否定 Scene Script，流程仍然可以只回退到 Scene Script，而不是全部重做。

### 3. “自动完成 Remotion Scene 实现”目前不可行

当前 Remotion 的时间线已经可以根据 Manifest 自动生成，但 Scene 视觉实现仍然是手写 TypeScript／TSX。

例如：

- Scene 配置需要手工定义。
- Scene 类型和字段需要手工匹配。
- 视觉 Reveal Segment 需要手工绑定。
- 复杂视频还会使用六个基础场景之外的专用表达。

因此第一版应该改成：

```text
自动生成：
- TTS Script
- Manifest
- Scene 时间轴
- audioTracks
- subtitleCues
- 基础 video.config.ts

Agent／人工完成：
- Scene 视觉实现
- 复杂视觉事件
- 视觉细节调整
```

除非以后建立严格的 Visual DSL，否则不能承诺“从 Visual Script 自动生成完整 Remotion 画面”。

### 4. 机器可读数据方向正确，但不应一开始复制所有 Markdown

方案建议同时增加：

```text
scene-script.md
scene-script.json

narration-script.md
narration-script.json
```

方向没问题，但如果 Markdown 和 JSON 都允许人工修改，就会产生双重事实源。

更稳妥的方式是：

```text
Markdown：
给人阅读和确认

结构化 JSON：
只保存需要程序校验的实体
```

第一版优先增加：

```text
production-state.json
tts-script.json
production-manifest.json
```

其中：

- `production-state.json` 管流程状态。
- `tts-script.json` 管 TTS 交接。
- `production-manifest.json` 管 Scene、Segment 和版本关系。

不建议一开始为每个 Markdown 都维护一个完全平行的 JSON 文件。

### 5. TTS 接口还没有真正确定

当前项目已有 TTS 结果，但更像是外部生成后复制进来的资料。

当前的 [tts-script.json](/Users/limiao/personal/2-topic/4-AI/project/video/local/claude-code-install/tts-script.json:1) 主要包含：

```text
videoId
sceneId
segment id
text
```

它还没有升级方案中要求的：

```text
voice
rate
generationId
narrationHash
manifestVersion
buildId
```

同时现有 [audio-manifest.json](/Users/limiao/personal/2-topic/4-AI/project/video/src/videos/claude-code-install/generated/audio-manifest.json:1) 使用的是：

```json
"rate": "+50%"
```

这不能直接证明当前 TTS 已经按照 1.25 倍语速工作。

所以 TTS 自动化前必须先确认：

- TTS 项目的实际路径。
- 输入命令或 API。
- 语速参数含义。
- 输出目录。
- 是否支持按 Segment 生成。
- 是否支持只重生成一个 Segment。
- 字幕和时间数据是否基于最终加速后的音频。
- 失败时如何返回错误。

这是整个方案当前最大的外部依赖。

### 6. 失效传播可行，但不能只标记当前 Segment

方案中这一点是正确的：

```text
03-02 narration 修改
→ 03-02 audio 失效
→ 03-02 subtitle 失效
→ Scene 03 timeline 失效
```

但还需要补充：

如果前面的 Segment 时长变化，后续所有全局时间偏移都要重新计算：

```text
03-02 音频变化
→ Scene 03 后续 Segment offset 变化
→ Scene 04 及之后的全局 offset 变化
→ Remotion Preview 失效
→ Smoke Render 失效
→ Full Render 失效
```

因此应采用：

```text
局部重新生成
+
全局时间轴重新计算
```

不能简单地只替换一段 MP3。

### 7. GitHub 自动化可行，但还差触发和监控载体

现有 GitHub Actions 已经证明远程渲染可行，但升级方案还需要解决：

- 如何传入 `videoSlug`。
- 如何传入 Composition ID。
- 如何上传或获取新视频资源。
- 如何触发 Workflow。
- 如何取得 Run ID。
- 如何读取运行状态。
- 如何下载 Artifact。
- 如何处理 GitHub Token 权限。
- 如何实现每 20 分钟轮询。

当前 Workflow 只是手动 `workflow_dispatch`，没有动态输入。

另外，“每 20 分钟查看”不能依赖一次对话永久后台运行。需要明确一种执行载体，例如：

```text
video pipeline monitor <slug> --run-id <id>
```

或者：

- 本地定时任务。
- GitHub Scheduled Workflow。
- 用户在 20 分钟后恢复会话。
- GitHub 状态通知触发后继续处理。

外部服务器信息不完整时，方案中“停止猜测并要求用户提供日志”是正确的。

## 推荐的修正版实施顺序

### 第一阶段：先打通一条视频的半自动流程

只做一条 `narrated` 视频：

```text
源文档
→ Agent 生成生产资料
→ Gate 1
→ Agent 生成口播和视觉资料
→ Gate 2
→ TTS
→ Manifest 校验
→ Remotion 时间线
→ Studio 预览
→ Gate 3
→ GitHub Smoke
→ GitHub Full
→ Gate 4
```

这一阶段不要先做完整 Pipeline Controller。

### 第二阶段：增加最小状态管理

增加：

```text
production-state.json
```

但状态至少应包含：

```text
videoSlug
buildId
stage
status
approvedGate
approvedArtifactHash
narrationHash
manifestVersion
commitSha
runId
lastError
```

人工批准不能只记录：

```json
"approvedGates": ["content-plan"]
```

还要记录批准时对应的文件 Hash，防止文件修改后旧批准仍被当成有效。

### 第三阶段：增加确定性校验器

优先做：

- Scene ID 检查。
- Segment ID 检查。
- TTS Script 和音频数量检查。
- Manifest 文件存在性检查。
- 字幕时间范围检查。
- Timeline 连续性检查。
- 音频总时长检查。
- Composition 注册检查。
- CJK 字体检查。
- GitHub 输出视频的 `ffprobe` 检查。

这些是最适合自动化、收益最高的部分。

### 第四阶段：打通 TTS Adapter

等 TTS 项目接口确认后，再实现：

```text
generate TTS Script
→ 调用 TTS
→ 接收结果
→ 校验 generationId／narrationHash
→ 写入 Video 项目
```

不要在 TTS 接口未确定前先编写假调用层。

### 第五阶段：GitHub Actions 参数化

将现有固定配置改为：

```text
videoSlug
compositionId
assetPackage
buildId
renderMode
```

同时增加：

- Smoke 自动验证。
- Full Render 前置条件。
- `ffprobe` 验证。
- Artifact 下载。
- Run 状态记录。

### 第六阶段：最后再做失效传播和 Pipeline Controller

失效传播需要前面已经稳定：

- 文件版本。
- Manifest 版本。
- TTS 交付协议。
- Remotion 配置生成。
- GitHub Run 记录。

否则 Controller 会把不稳定的规则自动化，排错成本反而更高。

## 最终判断

这份 [VIDEO-END-TO-END-AUTOMATION-UPGRADE.md](/Users/limiao/personal/2-topic/4-AI/project/video/VIDEO-END-TO-END-AUTOMATION-UPGRADE.md:1) 作为目标架构是可行的，尤其是这三个核心方向值得保留：

```text
Production State
Machine-readable Manifest
Validator + Gate + Invalidation
```

但需要把目标调整为：

> 第一版实现“Agent 负责创作，程序负责确定性编排和校验，人负责 Gate 确认”。

而不是第一版就实现：

> 程序从源文档自动生成所有内容，并自动完成 Remotion Scene 实现。

当前最合理的第一步是先确认 TTS 项目的真实接口和 GitHub 触发／监控方式，然后再决定如何更新项目规范和进入实施。此次审查没有修改任何文件；当前工作区原有的 `CLAUDE.md`、`ROADMAP.md` 修改和未跟踪文档均保持不变。
