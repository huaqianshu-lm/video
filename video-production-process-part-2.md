# 我是怎么做视频的（二）：从重新拆 Scene 到把视觉样片做完整

上一篇里，我记录了第一次做出视频后，为什么觉得画面像 PPT，以及我怎样先用两个场景验证「过程化画面」这个方向。

但方向确认以后，还有一个更具体的问题：一条解释型视频怎样在接入 TTS 前，先把观众的理解路径、画面过程和节奏固定下来。本文复盘的是我如何重新审查原有结构、把 6 个大场景拆成 14 个视觉事件，并通过 Scene Script、Visual Script 和静音预览逐幕确认画面。读完后，读者可以把这些判断用于自己的解释型视频：什么时候该拆 Scene，什么时候该把同一个问题做成对比，以及什么时候视觉表达已经可以交给声音进入时间轴。

这不是所有视频都必须遵循的唯一流程。这里讨论的是一种特定场景：在接入 TTS 前，为一条需要解释工具、工作方式和协作边界的视频，先固定视觉叙事。

## 为什么不能只给原有 Scene 加动画

发现「视频像 PPT」以后，我没有马上把所有卡片换成更复杂的动画，而是先打开之前的审查记录，逐幕重新看了一遍。

我当时主要问自己三个问题：

1. 这一幕到底想让观众理解什么？
2. 一幕里面是不是塞了太多知识点？
3. 画面是在展示一个过程，还是只是在列出结论？

看完以后，我发现问题不在基础组件本身。`OpeningScene`、`ConceptScene`、`ComparisonScene`、`StepListScene`、`TerminalScene` 和 `SummaryScene` 仍然可以复用，但我之前让每个场景承担的内容太多了。

比如对比场景同时讲两种工作方式，结尾场景又同时讲工具类比、适用范围和最终结论。为了让这些内容都出现，我只能不断往画面里加卡片。此时继续给卡片加动画，只会让同一个拥挤的问题动起来，不能减少一幕里的认知负担。

所以这一次我没有先重写组件，而是先重新安排 Scene 的结构。后来我把「先把动画拿掉再看一眼」留成了一个简单判断：静下来以后，这一幕如果仍然说不清观众到底该理解什么，问题通常不在动画效果，而在 Scene 本身承担得太多。

## 为什么要把 6 个大场景拆成 14 个视觉事件

我重新整理观众的理解顺序，让视频从一个具体问题开始，而不是直接从定义开始：

```text
01 Bug 出现
↓
02 ChatGPT 工作流
↓
03 Claude Code 工作流
↓
04 Advice → Action
↓
05 Claude Code 定义
↓
06 Copilot → Agent
↓
07 理解项目
↓
08 执行任务
↓
09 连接外部工具
↓
10 能不能全部交给 AI？
↓
11 AI 不能替代判断
↓
12 人机重新分工
↓
13 工具如何选择
↓
14 AI 编程方式发生变化
```

我把场景拆多，并不是为了让视频显得更复杂，而是因为每一个节点都代表一次认知变化。前面几幕先让观众感受到传统工作流为什么绕；中间几幕再展示 Claude Code 如何进入项目、理解代码和执行任务；最后我补上「能不能全部交给 AI」这个问题，把人负责判断、AI 负责执行的边界讲清楚。

拆完以后，Scene 数量从 6 个变成了 14 个，但每一幕反而更容易确定画面应该发生什么。我不再用「这一段还能塞进多少内容」来衡量一个 Scene，而是看它能不能只让观众完成一次新的理解。定义、过程和结论都想放进同一幕时，卡片会越加越多；切到下一幕以后，重点也往往没有真正变过去。这种时候，继续拆成视觉事件通常比继续补说明更有效。

## 为什么同一个 Bug 更适合做两种工作流的对比

在重写对比部分时，我没有把 ChatGPT 和 Claude Code 各做成一张功能介绍页。我给两边安排了同一个 Bug，让观众看同一件事在两种工作方式下分别怎么完成。

### 我先做传统工作流

我把这一侧安排成一条不断往返的操作路径：

```text
IDE
→ 复制报错
→ ChatGPT
→ 返回 IDE
→ 找文件
→ 复制代码
→ ChatGPT
→ 手动修改
→ 重新运行
```

我没有把每一步做成一张说明卡，而是让 IDE 和聊天窗口之间的来回切换成为画面的主体。步骤越往后，操作轨迹越长，观众自然能感觉到项目和 AI 之间隔着一段距离。

### 我再做 Claude Code 工作流

另一侧使用同一个问题，但把任务放进项目里连续执行：

```text
输入一个任务
→ 搜索文件
→ 读取代码
→ 检查引用
→ 修改文件
→ 运行测试
→ Tests passed
```

这样口播不用再抽象地说「Claude Code 更接近 Agent」，画面本身已经把两种工作方式的差别展示出来了。

同一个 Bug 让两边拥有相同的起点，观众看到的差异才会落在工作流，而不是两个不同案例上。对比场景最怕两边各说各话：没有共同任务，画面很容易又退回成两列功能描述；把共同的起点保留下来，操作过程本身就会说明差别。

## 为什么要先写 Scene 的任务，再写画面

结构重排之后，我重新写 `scene-script.md`。这一次我没有先想「这一幕放几张卡」，而是先给每个 Scene 写一个明确任务：这一幕为什么存在，观众看完以后新增了什么理解。

我会同时记录四件事：

- 这一幕的认知目的。
- 口播需要解释的内容。
- 画面需要展示的过程。
- 为什么这件事必须用视频表达。

例如「同一个 Bug，Claude Code 怎么处理」这一幕，我没有让口播逐项念出「读取文件、搜索引用、修改代码、运行测试」。我把口播压缩成一个目标：

> 帮我找到这个报错的原因，然后修掉。

后面的读取、搜索、修改和测试全部交给画面去证明。这样声音负责提出任务和解释意义，画面负责展示任务真的被执行了。

这一步让我再次确认，口播和视觉脚本不能各自把同一件事讲一遍，否则最后还是字幕读一遍、卡片再读一遍。后来写脚本时，我会先把口播压回目标、因果或意义，再看画面是否真的留下了读取、搜索、修改、测试这些只有视频才能证明的过程。

## 为什么 Agent 要被做成工作过程，而不是定义卡片

「Agent」是这条视频里最容易重新做成定义卡片的部分。为了避免这一点，我先不急着解释名词，而是先展示人与 AI 的控制方式发生了什么变化。

我把旧的工作方式整理成：

```text
人发现问题
→ 人拆下一步
→ 问 AI
→ 人执行建议
→ 再问 AI
```

新的工作方式则是：

```text
人提出目标
→ Agent 找文件、分析、修改、运行
→ 人查看结果并验收
```

画面中，我让「人一直管理每一步操作」逐渐变成「人给出目标，AI 承担中间执行」。这样 Agent 的变化是从过程里看出来的，不需要依赖一张抽象定义页。

这个场景做完以后，我更确定了一件事：涉及角色、控制方式或任务流转的概念，先让观众看见前后过程的差异，比先递出定义更有说服力。画面如果只能复述定义，观众仍然只能靠文字理解；过程一旦发生变化，定义才有可见的依据。

## 怎样让每项能力都有一个可见的过程

后面的能力场景，我也没有继续做功能清单，而是给每个能力找一个代表性过程。

### 理解项目

我让陌生项目的文件树先被扫描，再从文件关系里整理出：

```text
UI → Service → API → Database
```

我想表达的是「它在建立项目关系」，而不是简单写一行「支持代码分析」。

### 执行任务

我用一个连续任务表现修改过程：

```text
Refactor auth module
→ auth.ts Modified
→ login.ts Modified
→ auth.test.ts Created
→ npm test
→ 24 passed
```

重构、补测试、运行检查这些内容如果分开做成卡片，画面很快又会变成能力列表。所以我让它们发生在同一个任务里，让口播概括范围，画面演示结果。

### 连接外部工具

我把 Claude Code 放在中间，依次连接 `Codebase`、`Docs`、`Jira`、`Slack` 和 `Drive`，表达工作范围从代码库向项目资料延伸。

这里我没有再制作每个工具的详细操作，因为这一幕的重点是关系发生变化：原来 AI 只在代码窗口里回答问题，现在它可以把项目里的不同资料连接到同一个任务中。

做到这里，我不再把「能否再加一个功能名」当成画面是否完整的标准。能力要么落在一个看得见状态变化的过程里，要么像外部工具这一幕一样，只展示本来要说明的连接关系；没有必要的过程，就不该为了显得完整而虚构细节。

## 为什么视频需要把「不能全交给 AI」做成一个关卡

如果视频只展示 AI 能理解、能执行、能连接，观众很容易顺着结论走到「那以后是不是可以把项目全部交给 AI」。所以我专门安排了一幕，把这个问题直接说出来：

```text
既然 AI 已经能做这么多，项目是不是可以全部交给它？
```

下一幕里，我让 Claude Code 给出 `Pros / Cons`，但最终的 `Decision` 仍然留在人这一侧。即使画面出现：

```text
Tests passed ✓
```

我还要继续追问：

```text
Business correct？
```

这样「人负责判断，AI 负责执行」不是最后补上的一句口号，而是任务流程中的一个真实关卡。仅有 `Tests passed`，只能证明工具把一项技术任务做完；把 `Business correct？` 留下来，观众才会看见那个完成状态替代不了什么判断。

## 组件可以复用，为什么画面不能重复

Scene 从 6 个增加到 14 个以后，我仍然没有为每一幕单独创建一个新组件。不同 Scene 主要复用下面几种表达方式：

- `Opening`：建立问题和视觉钩子。
- `Concept`：解释项目关系或抽象概念。
- `Comparison`：表现两种工作方式的差异。
- `StepList`：展示任务推进。
- `Terminal`：展示执行日志和检查结果。
- `Summary`：收束结论。

具体的文件名、命令、状态和屏幕文字放进视频配置；组件只负责布局、颜色、动画和渲染逻辑。这样我调整某个 Scene 时，通常先修改配置和视觉脚本，而不是为了换一行代码文字就重写组件。

我这时才把「复用组件」和「画面不重复」分开：组件可以复用，但每个 Scene 必须有自己的任务、状态或视觉事件。后来遇到新的 Scene，我先看现有组件能否承载这次要表达的过程；如果只是文件名、状态或屏幕文字变了，就把变化留在配置和脚本中，而不是为了换一行内容再造一个组件。

## 静音时，怎样确认画面本身有节奏

在还没有接入 TTS 以前，我先把它当成一条静音视觉样片来做。场景时长不再简单套用固定总时长，而是按画面本身估算：

- 主要元素完成入场需要多久。
- 文件树、代码或终端状态需要多久才能看懂。
- 一个视觉事件是否需要短暂停留。
- 总结内容是否需要额外的阅读和思考时间。

我特别注意列表、终端输出和总结画面，不能在开头几秒全部出现，然后剩下的时间只停在最终状态。我让画面按照任务的推进顺序逐步展开。

总结画面也不能文字刚出现就切走，所以我给最后的观点保留了几秒静置时间。静音预览时，我会直接检查：关掉声音后，是否还能看出任务从哪里开始、经过了什么状态、停在了什么结果；关键文字和终端结果是否有足够时间被看清。如果答案是否定，问题应先在视觉节奏里解决，而不是等待音频来填补空白。

## 为什么要逐幕确认，而不是一次性做完整条视频

正式实现时，我没有一次性把完整视频写完，而是按 Scene 往前推进：

1. 先实现这一幕最主要的视觉动作。
2. 在 Studio 里看布局、文字、节奏和动作含义。
3. 发现问题就先改这一幕。
4. 确认方向后再继续下一幕。

后半段的概念和总结场景尤其容易信息过量。比如工具对比不能一次放进太多关系，判断边界需要给 `Tests passed` 和人工 Review 留停顿，总结画面要等完整展开以后再停留。

我会先把已经确认的场景当作稳定部分，再继续往后做。这样如果后面某一幕出了问题，我知道应该回到哪一层修改，不会整条视频一起返工。逐幕确认时，我关注的是很具体的事：这一幕的主视觉动作是否先于次要信息被看懂；文字、状态和停顿是否仍服务于这一幕唯一的认知任务。只有这两个判断清楚，下一幕才有稳定的前提。

## 为什么要在接入 TTS 前冻结视觉表达

进入 TTS 以前，我先把下面这些内容固定下来：

- 14 个 Scene 的顺序和认知任务。
- 每一幕的口播方向和信息边界。
- 每一幕的主视觉动作。
- 文件名、命令、状态等关键屏幕文字。
- 场景之间的进入和退出关系。
- 哪些信息由口播解释，哪些信息必须由画面展示。

我把这一步理解成先把「视频要表达什么」和「画面应该怎么发生」锁定，再让声音进入时间轴。这样后面的 TTS 是给已经确定的叙事配时间，而不是让一段音频反过来决定视频要讲什么。

冻结前，我会回看前面的静音样片：Scene 顺序是否已经能连成完整的理解路径；每幕的口播和画面分工是否已经明确；关键屏幕文字和进退场关系是否还会改变。若这些还没有定下来，声音一旦进入时间轴，修改就会同时牵动口播、节奏和画面。反过来，视觉表达已经稳定时，TTS 才是在为已有叙事提供时间依据。

## 这一阶段的总结

从视觉方向确定到 TTS 之前，我实际走的是这条路径：

```text
重新审查旧 Scene
→ 按认知变化重排
→ 拆成 14 个视觉事件
→ 写 Scene Script
→ 写 Visual Script
→ 为每幕寻找具体过程
→ 复用基础组件实现
→ 先做静音预览
→ 逐幕确认和修改
→ 冻结 Scene 与视觉表达
→ 再进入 TTS
```

这条路径最后沉淀成了三条可以迁移的判断原则：

1. 一个 Scene 只承担一次认知变化。Scene 变多不一定让视频复杂；当每一幕都能说清观众新增了什么理解，画面反而更容易设计。
2. 口播提出任务、解释意义，画面证明过程。像同一个 Bug 的两种工作流、文件扫描、修改和测试，应该让观众在状态变化中看见，而不是由声音和卡片重复念出。
3. 先确认无声画面，再让声音进入时间轴。先用静音版本确认视觉动作、信息停留和场景关系，才能让 TTS 为已经稳定的叙事配时间。

对我来说，这一阶段最重要的变化，不是 Scene 数量从 6 个变成了 14 个，而是终于把「内容、画面过程和视觉节奏」先做成立，再让声音进入时间轴。

## 参考资料

- [Scene Script](videos/claude-code-what-is/scene-script.md)
- [Visual Script](videos/claude-code-what-is/visual-script.md)
- [视频审查记录](videos/claude-code-what-is/reviews/video-audit.md)
- [视频制作问题与经验](docs/video-production-notes.md)
- [第一部分：从开始到解决「视频像 PPT」](video-production-process-part-1.md)
