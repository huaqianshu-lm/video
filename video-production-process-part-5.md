# 视频是怎么做的（五）：渲染命令成功了，为什么视频还不能直接交付？

前面的几篇已经把内容、画面、TTS、字幕和时间轴接好了。

这一篇，我要把它们真正变成一份可以交付的 MP4。

我一开始很容易把渲染理解成「最后执行一个命令」。但真正做过以后，我发现渲染并不是按下回车以后等文件出现这么简单。渲染前要先确认视频已经冻结，渲染环境要有完整素材和中文字体，渲染过程中还要先用短结果排除问题，最后拿到 MP4 以后也要检查文件和画面。

这次我遇到的关键问题是：本地 Studio 能看到画面，不代表本地一定能稳定渲染；本地命令执行成功，也不代表远端 Runner 里一定有音频、字幕和字体。

所以我最后采用的顺序是：

```text
Studio 预览确认
→ 确认素材和视频规格
→ 检查渲染环境
→ 运行 Smoke Test
→ 检查短片和代表帧
→ 渲染完整 MP4
→ 检查最终文件
→ 交付
```

这一篇记录我怎样处理最后这道关卡。

**看完这一篇，你会拿到：**

- 一套从 Studio 预览到 MP4 交付的渲染流程。
- 一种在正式渲染前检查视频规格、素材、字体和依赖的方法。
- 一种先跑代表帧和前 10 秒短片，再决定是否跑完整视频的方式。
- 一份拿到 MP4 后需要检查的文件规格、字体、字幕和音画同步清单。

> 💡 **一句话总结**：渲染命令成功，只说明产物生成了；最终交付还需要证明这份产物能播放、看得清、听得对。

## 01 先等视频冻结以后再渲染

我不会每改一次画面就渲染一次完整视频。

正式渲染以前，我先确认几个会影响最终结果的东西：

- Scene 顺序已经确定。
- 口播、音频和字幕时间轴已经接入。
- 主要视觉动作已经和 Segment 对齐。
- 中文字体、字幕边界和音频开头已经在 Studio 里检查过。
- 总结画面保留了足够的阅读和思考时间。
- 当前 Remotion 预览已经通过人工确认。

这些内容如果还没有确认，我会继续改配置、组件或时间轴。因为 MP4 是最后的输出，不应该被我拿来发现本来可以在 Studio 里解决的问题。

这和前面接入 TTS 时的排查顺序是同一个原则：先把上游资料和画面逻辑确认好，再进入成本更高的环节。

> 💡 **一句话总结**：渲染是交付阶段，不是用来替代 Studio 预览的调试阶段。

## 02 再确认分辨率、帧率和总时长

视频冻结以后，我再确认视频规格。

当前正式视频按横屏教程规格制作：

```text
1920 × 1080
30fps
```

视频总时长则来自真实时间轴，而不是提前写死一个秒数。对这条带有人工音频的版本来说，Scene 的起止时间来自音频和 Manifest，最后一幕在口播结束后还保留了 3 秒的静默停留。

这意味着我不能只检查宽高和帧率，还要确认总帧数是否来自当前配置。如果 Composition 指向了旧配置，画面可能看起来是对的，时长却仍然是早期的估算版本。

我把这几个值看成渲染输入的一部分：

```text
宽度和高度 → 画面是否按横屏构图输出
帧率       → 秒和帧是否仍然对应
总帧数     → 音频、字幕和最后一幕是否完整结束
```

> 💡 **一句话总结**：渲染规格不只是输出参数，它也决定了时间轴能不能完整落地。

## 03 先检查依赖和音频资源

我会先检查 Node.js 和项目依赖，再运行 TypeScript 检查：

```bash
node --version
npm ci
npm run check
```

这个项目当前使用 Node.js 22，TypeScript 继续保持 `~5.8.3`。如果依赖安装或类型检查没有通过，我不会直接进入渲染。

然后我会单独检查音频、字幕和 Manifest。

本地原始资料和 Remotion 运行时可以读取的资源，不是同一个目录。当前项目的分工是：

```text
local/claude-code-install/
→ 保存本地原始资料

public/local-assets/claude-code-install/
→ 提供 Remotion 运行时读取的音频和字幕

src/videos/claude-code-install/generated/
→ 保存供 TypeScript 消费的 Manifest
```

渲染前，我会核对：

- `captions.vtt` 是否存在。
- 音频目录是否存在。
- MP3 数量是否符合当前时间轴。
- Manifest 中的路径是否都能找到对应文件。
- Segment ID 是否仍然和音频、字幕一一对应。

当前工作流会检查 `claude-code-install` 的字幕文件，并确认音频数量为 76 段。这个检查看起来很基础，但它能提前发现一个很常见的问题：代码已经提交了，`.gitignore` 忽略的本地音频却没有进入 Runner。

如果 Runner 里没有这些文件，渲染失败时我就会误以为是 Remotion 组件的问题。先验证资源是否存在，可以把“代码问题”和“交付资料没有准备好”分开。

> 💡 **一句话总结**：远程渲染前，代码、Manifest 和被忽略的本地素材必须作为一个整体检查。

## 04 考虑过直接在本地执行渲染

最直接的方式是在本地运行：

```bash
npm run render
```

也可以直接指定 Composition 和输出路径：

```bash
npx remotion render claude-code-install out/claude-code-install.mp4
```

本地 CLI 的优点很明显：错误信息直接，本地素材不用重新打包，改完一个配置以后可以很快尝试一次。

但我实际遇到了环境问题：本机旧版 macOS 上的 Remotion Chromium 启动时返回 `SIGTRAP`。这不是视频配置的问题，也不是中文字体的问题，而是本地浏览器渲染环境本身无法稳定启动。

我没有继续在本机反复重试同一个命令。因为在没有新的环境证据时，重复执行只能重复得到同一个结果。于是我把本地 CLI 的定位收窄为：

```text
本地 CLI
→ 快速调试和定位渲染错误

统一 Linux 环境
→ 生成这次最终交付的 MP4
```

这一步对我来说很重要：工具没有失效，只是它不再适合作为当前环境下唯一的最终交付入口。

> 💡 **一句话总结**：本地渲染环境不稳定时，先区分“视频配置问题”和“浏览器运行环境问题”，再决定交付位置。

## 05 把 Studio 当成人工确认关卡

本地无法稳定生成 MP4，并不意味着本地 Studio 没有价值。

Studio 对我来说主要是人工预览工具。我会先启动：

```bash
npm run preview
```

然后逐幕检查：

- 画面有没有完整出现。
- 第一条音频有没有从正确的位置开始。
- 字幕是不是跟着语句切换。
- 关键动作是不是在对应 Segment 出现。
- 中文字体是不是正常。
- 总结画面有没有足够停留。

Studio 很适合定位某一帧的问题，也适合我根据反馈快速改配置。但 Studio 里能看到画面，不代表 Linux 渲染环境里的字体、素材和 Chromium 一定相同。

所以我把 Studio 定义成一道人工作品确认关卡：它负责确认内容、节奏和画面表达，统一 Runner 负责生成最终文件。

这也让“预览通过”和“最终交付”成为两个不同的判断：前者是我确认视频已经值得渲染，后者是我确认渲染出的文件可以交付。

> 💡 **一句话总结**：Studio 负责回答“画面是否正确”，最终渲染负责回答“文件是否可交付”。

## 06 把最终渲染放进 GitHub Actions

本地环境不稳定以后，我把渲染流程放进 GitHub Actions，并拆成两个手动触发的工作流：

```text
Smoke test video
→ 先渲染代表帧和前 10 秒

Render full video
→ 确认冒烟结果以后再渲染完整 MP4
```

我没有让每次提交都自动渲染，而是在确认需要渲染时手动触发。因为当前目标是完成一条样片交付，不是每次改一个字都重新生成整条视频。

GitHub Actions 给了我一个比本机更稳定的执行环境：Node.js 版本固定，中文字体可以提前安装，依赖和本地素材也可以按同一套步骤准备。

但它不会自动拥有我本机的 `local/` 和 `public/local-assets/`。这两个目录按项目规则不依赖 Git 提交，所以素材需要另外准备。

> 💡 **一句话总结**：统一环境的价值不只是“换一台机器”，而是把依赖、字体和素材准备步骤固定下来。

## 07 先把被 Git 忽略的素材准备给 Runner

这次远程渲染前，我把当前视频需要的本地资源整理成：

```text
assets/claude-code-install-assets.zip
```

工作流启动后，先创建目标目录，再解压素材，最后检查字幕文件和 MP3 数量：

```text
创建 public/local-assets/
→ 解压视频资源 ZIP
→ 检查 captions.vtt
→ 检查 MP3 数量
→ 运行 TypeScript 检查
```

我没有把这一步当成渲染脚本里的隐含细节，而是把它作为一个单独的输入检查步骤。这样工作流失败时，日志可以直接告诉我是素材缺失，还是代码没有通过。

对这类有本地音频的视频来说，素材包本身就是交付的一部分。只把源码交给 Runner，而不把 Manifest 指向的 MP3 和字幕一起准备好，后面的渲染命令就没有完整输入。

> 💡 **一句话总结**：被 Git 忽略的素材不会因为代码进入仓库就自动进入渲染环境。

## 08 在服务器渲染时遇到中文字体问题

素材进入 Runner 以后，我遇到的下一个问题不是代码，而是字体。

本地预览里的中文是正常的，但 Linux 服务器并不会自动拥有本机的中文字体。这条视频包含中文标题、中文字幕、文件树和终端文字，如果服务器没有合适的 CJK 字体，最终画面就可能出现方框、字形替换或布局变化。

所以第一次准备服务器渲染环境时，我先安装字体：

```bash
sudo apt-get install -y fonts-noto-cjk
```

安装以后，再用 `fc-match` 检查 `Noto Sans CJK SC` 是否真的能被系统找到。不是安装命令返回成功就结束，而是要确认字体查询结果符合预期。

这次问题让我意识到，本地 Studio 里看到中文正常，只能证明本地环境正常，不能证明服务器环境正常。渲染时遇到的字体问题，也要像音频资源一样提前变成工作流里的失败条件：字体检查没有通过，工作流就直接停止，不继续生成一份看起来完成、实际却无法交付的视频。

我后来把服务器渲染前的输入检查收拢成三层：

```text
Manifest 存在
→ 素材存在
→ 字体存在
→ 才进入画面渲染
```

> 💡 **一句话总结**：本地预览正常，不代表服务器里的中文正常；远程渲染环境也必须被单独验证。

## 09 先跑 Smoke Test，而不是直接跑完整视频

完整视频很长，直接跑全片的反馈周期比较长。为了避免用一次完整渲染来排查基础问题，我先跑 Smoke Test。

当前冒烟工作流会做三件事：

```text
渲染开头代表帧
→ 渲染中间代表帧
→ 渲染后半段代表帧
→ 渲染前 10 秒短片
```

三个代表帧分别覆盖视频开头、中间和后半段。它们可以比较快地暴露：

- 中文字体是否正常。
- 主文案和字幕是否溢出。
- 字幕顶层是否仍然可见。
- 长时间线中间是否出现空画面。
- 后半段 Scene 是否仍然按配置渲染。

前 10 秒短片则用来检查最容易出错的动态部分：第一条音频有没有从正确位置开始，第一组字幕是否出现，开场动画和声音是不是同时进入。

这次冒烟工作流成功后，它生成的代表帧为 1920 × 1080，前 10 秒短片为 1920 × 1080、30fps、H.264 + AAC，时长为 10.048 秒。

冒烟结果通过以后，我才继续进入完整渲染。

> 💡 **一句话总结**：Smoke Test 的作用是缩短反馈周期，先证明关键位置能渲染，再承担完整视频的成本。

## 10 再跑完整渲染

完整工作流重复使用同一份素材 ZIP、同一套字体安装和同一份依赖，然后执行：

```bash
npx remotion render claude-code-install out/claude-code-install.mp4
```

这里的重点不是命令本身，而是它和 Smoke Test 使用同一套输入准备流程。否则冒烟阶段能看到的环境，和完整渲染阶段实际使用的环境可能并不一样。

完整工作流完成以后， 把 MP4 下载下来。

到这里，我才算得到一份真正从代码、素材和统一环境生成的 MP4。工作流显示绿色，只说明任务完成；下一步还要检查文件本身。

> 💡 **一句话总结**：完整渲染应该复用已经通过冒烟检查的输入准备流程，避免前后环境不一致。

## 11 拿到 MP4 后还要再检查一次

渲染命令成功，只说明进程结束了，不代表视频已经可以交付。

我下载 MP4后，先检查文件是否存在、能不能播放、是否有视频和音频轨道，再检查规格：

```text
1920 × 1080
30fps
H.264 + AAC
48kHz 双声道
```

然后再看画面和音画关系：

- 中文有没有变成方框。
- 字幕有没有被裁切或被场景盖住。
- 主文案、文件树、代码和终端是否仍然清楚。
- 每个 Scene 的第一句话是否完整。
- 字幕有没有整体提前或延后。
- 画面动作是否仍然对应口播语义。
- 最后一幕的音频、字幕和总结画面是否完整结束。

我把这一步看成最后一次验收，而不是重复预览。Studio 里检查的是制作过程中的画面，MP4 检查的是实际交付出去的文件。

> 💡 **一句话总结**：绿色的工作流不是交付证明，文件规格和实际画面检查才是。

## 12 没有在第一条样片里引入云渲染

本地环境遇到问题以后，我也考虑过 Remotion Lambda 或其他云渲染服务，但第一条样片没有采用这条路线。

因为云渲染还会带来另一组问题：

```text
云账号和权限
→ 对象存储
→ 素材上传
→ 任务队列
→ 失败重试
→ 成本控制
```

当前第一阶段要验证的是：一条视频能不能从生产资料走到最终 MP4。GitHub Actions 已经提供了统一的 Node.js、字体、依赖和素材环境，足够完成这次交付。

所以我暂时不为了“以后可能批量渲染”提前引入一套更大的系统。等真正出现批量渲染、并发任务或更长视频的需求时，再重新评估云渲染是否值得。

> 💡 **一句话总结**：当前只需要稳定交付一条样片时，统一工作流已经够用，规模化问题留到真正出现时再解决。

## 13 最后这样分工几种工具

经过这次排查，我给不同工具分了不同的职责：

```text
Studio
→ 确认画面、节奏、字幕和音频

本地 CLI
→ 快速调试，定位渲染错误

GitHub Actions
→ 在统一环境中生成可交付的 MP4
```

不是每次修改都要走完整流程。小范围调整时，我先用 Studio 或本地命令验证；当用户确认预览、需要留档或需要交付时，再使用 GitHub Actions 生成统一环境下的产物。

这条分工让每个环节都有清晰的判断标准：

|工具|我用它回答的问题|
|---|---|
|Studio|画面和节奏是否已经正确？|
|本地 CLI|当前配置能不能快速复现某个渲染问题？|
|GitHub Actions|在统一环境中能不能生成最终 MP4？|

> 💡 **一句话总结**：Studio 用来确认，CLI 用来调试，GitHub Actions 用来交付。

## 14 最后形成了一条可复用的交付流程

现在我不会再把“执行渲染命令”当成独立动作，而是按下面的顺序完成：

```text
用户确认 Studio 预览
→ npm run check
→ 确认视频规格和总时长
→ 检查字体和本地资源
→ 准备素材 ZIP
→ 运行 Smoke Test
→ 检查代表帧和前 10 秒
→ 运行完整渲染
→ 下载 MP4 Artifact
→ 检查文件、规格、画面和音画同步
```

如果中间某一步失败，我会先根据失败所在的层级处理：

```text
代码检查失败
→ 查配置和 TypeScript

资源检查失败
→ 查素材包、路径和 Manifest

字体检查失败
→ 查 Linux 渲染环境

代表帧或前 10 秒异常
→ 回到 Studio 和视频配置

完整 MP4 规格或画面异常
→ 回到渲染输入和最终文件检查
```

这样处理以后，渲染就不再是一个黑盒。它只是前面所有生产资料、时间轴和视觉确认结果的最后一次统一执行。

## 小结

我最后留下的经验是：

> Studio 用来确认，CLI 用来调试，GitHub Actions 用来生成统一环境下的交付产物；云渲染等真正需要规模化时再引入。

一条视频真正完成，不是因为命令返回了成功，而是因为我能确认：

```text
Composition 正确
→ 依赖和素材完整
→ 中文字体可用
→ 冒烟结果正常
→ 完整 MP4 规格正确
→ 画面、字幕和音频都能正常交付
```

前面几篇解决的是内容怎样变成时间轴和画面，这一篇解决的是这套结果怎样跨过环境差异，变成一份真正可以交付的文件。

## 参考资料

- [项目规范](CLAUDE.md)
- [当前进度](ROADMAP.md)
- [冒烟渲染工作流](.github/workflows/smoke-test-video.yml)
- [完整渲染工作流](.github/workflows/render-video.yml)
- [当前 npm scripts](package.json)
- [视频制作问题与流程沉淀](docs/video-production-notes.md)
- [第四部分：TTS 接入与音画同步](video-production-process-part-4.md)
