
你现在是当前项目的视频前期制作助手。请根据我提供的文章，生成一套符合 Remotion AI Video MVP 规范的视频前期生产资料。

开始前必须先读取：

1. ~/.claude/CLAUDE.md
2. 当前项目的 CLAUDE.md
3. 当前项目的 ROADMAP.md
4. docs/VIDEO-PRODUCTION-RULES.md
5. docs/VIDEO-PROJECT-WORKFLOW.md
6. 如涉及字幕、音频或场景节奏，再读取 docs/video-production-notes.md

如果没有新增项目规范，不修改 CLAUDE.md。本次任务只处理视频前期资料，不生成正式 Remotion 代码，不执行 npm install、npm run preview 或 npm run render。

## 视频基本信息

- 视频 slug：如果我没有提供，请根据标题生成 kebab-case slug
- 视频标题：
- 目标观众：
- 预计发布平台：
- 其他要求：

## 总体要求

- 视频规格按 16:9 横屏、1920 × 1080、30fps 设计。
- 视频时长由内容自然决定，不强行设置固定时长。
- 以文章事实为准，不擅自补充未经证实的信息。
- 将“原文事实”“合理推断”“待确认信息”明确区分。
- 按观众的认知变化拆分 Scene，而不是机械按段落拆分。
- 每个 Scene 必须有明确的认知任务和 Video Value。
- 优先使用以下场景类型：
  - OpeningScene
  - ConceptScene
  - ComparisonScene
  - StepListScene
  - TerminalScene
  - SummaryScene
- 视觉风格保持干净、科技感、克制、教程感，背景优先深色。
- 主文案尽量控制在 1～2 行，字幕控制在 1～2 行。
- 列表不超过 5～6 项。
- 不设计复杂 3D、粒子、电影级转场或与内容无关的装饰。
- 需要人工确认的地方标记为“待确认”，不要自行猜测。

## 必须按以下顺序执行

每完成一个阶段后：

1. 写入对应文件；
2. 汇报本阶段的核心结论；
3. 列出仍需确认的问题；
4. 停止工作，等待我确认后再继续。

不要跳过确认点，也不要一次性进入正式 Remotion 实现。

### 阶段一：Source 与 Content Analysis

生成：

- videos/<video-slug>/source.md
- videos/<video-slug>/content-analysis.md

Content Analysis 至少包含：

- 核心命题
- 目标观众的问题
- 文章知识骨架
- 关键概念及其关系
- 事实与证据
- 可视觉化内容
- 不适合直接视频化的内容
- 内容缺口和待确认信息
- 视频应该让观众最终理解什么

完成后停止。

### 阶段二：Video Narrative

用户确认后生成：

- videos/<video-slug>/video-narrative.md

至少包含：

- 开场钩子
- 观众痛点或问题
- 视频承诺
- 叙事主线
- 认知推进顺序
- 结尾总结
- 可选的下一集预告

完成后停止。

### 阶段三：Scene Script

用户确认后生成：

- videos/<video-slug>/scene-script.md

每个 Scene 至少包含：

- Scene ID
- Scene 标题
- 使用的场景类型
- 认知任务
- Video Value
- 本 Scene 要讲清楚的内容
- 主要口播意图
- 主要视觉事件
- 屏幕文字
- 场景进入和结束方式
- 初步时长依据

Scene 数量根据视觉事件和认知变化决定，不追求数量少，也不机械拆分。

完成后停止。

### 阶段四：Narration Script

用户确认后生成：

- videos/<video-slug>/narration-script.md

要求：

- 使用自然、适合口播的中文；
- 避免照搬文章书面语；
- 每句口播对应明确的信息点；
- 按 Scene 分组；
- 控制句子长度和阅读节奏；
- 不使用逐字字幕高亮；
- 如果没有真实音频，不伪造音频时长。

完成后停止。

### 阶段五：Visual Script

用户确认后生成：

- videos/<video-slug>/visual-script.md

每个 Scene 至少说明：

- 画面构图
- 主视觉元素
- 屏幕文字
- 软件界面、终端、文件树、代码、Diff 或状态卡片的具体内容
- 视觉状态变化
- 元素出现顺序
- 动画说明
- 字幕位置
- 信息密度风险
- 与口播的对应关系

Visual Script 只描述视觉表达，不写正式 React 或 Remotion 代码。

完成后停止。

### 阶段六：Visual Prototype

用户确认后生成：

- videos/<video-slug>/visual-prototype.html

要求：

- 使用无依赖 HTML、CSS 和必要的少量 JavaScript；
- 按 1920 × 1080 的 16:9 构图设计；
- 可以切换或查看主要 Scene；
- 展示关键画面结构、屏幕文字、状态变化和视觉事件；
- 不追求最终动画还原；
- 不引入外部库；
- 确保文字不明显溢出、字幕可读、画面信息不过载。

完成后停止，等待我确认 Visual Prototype。

在我确认 Visual Prototype 之前，不得生成或修改：

- src/videos/<video-slug>/video.config.ts
- 正式 Remotion 主组件
- 正式场景组件
- 渲染配置
- MP4 文件

## 输出方式

如果当前环境可以写入项目文件，直接按约定路径写入；如果不能写文件，则按照文件名分节输出完整 Markdown 或 HTML 内容。

每个阶段结束时只汇报：

- 本阶段生成或修改的文件；
- 3～5 条核心结论；
- 待确认问题；
- 下一阶段是什么。

## 文章正文

--- 原文开始 ---

在这里粘贴文章全文。

--- 原文结束 ---
```

每个阶段确认后，可以只回复：

确认，继续下一阶段。
```

这个提示词会先完成内容分析，再逐步生成叙事、Scene、口播稿、视觉脚本和 Visual Prototype，不会在视觉原型确认前提前进入 Remotion 实现。