# 我是怎么做视频的（一）：从做出第一版到解决「视频像 PPT」

这篇记录我第一次通过提示词让 Claude Code 协助制作 `Claude Code 到底是什么？` 教程视频的过程。

我一开始没有直接要求 Claude Code 把文章做成完整视频，而是先把目标拆成几个可以逐步确认的问题：这篇内容到底要讲什么，观众应该按照什么顺序理解，哪些内容适合用画面展示，以及 Remotion 的技术链路能不能跑通。之后，我让 Claude Code 分阶段完成内容分析、脚本和实现，再根据预览结果继续补充提示词，逐步校准方向。

我想验证的是：能不能把一篇关于 Claude Code 的文章做成一条真正的视频，而不是把文章改成口语，再配几张会动的卡片。实际让 Claude Code 完成第一版以后，我发现画面虽然动起来了，但看上去还是很像 PPT。于是我继续根据预览结果给出更具体的反馈，让它重新检查内容、Scene 和画面表达，慢慢把方向调整成「展示过程」。

这一篇先写从第一版到解决「视频像 PPT」的部分，后面的配音、字幕同步和最终渲染再单独记录。

## 视频主题

我先把视频主题和原始文章交给 Claude Code，让它把文章中可以讨论的内容范围列出来。但我没有直接让它按照这些内容写成一条视频，而是在提示词里要求它先围绕观众最终需要获得的理解，梳理哪些信息必须保留、哪些内容可以后置。

第一个主题是「Claude Code 到底是什么？」。

原始内容里有很多东西可以讲：它是什么、能做什么、在哪些平台使用、和 ChatGPT／Copilot／Cursor 有什么区别、不能做什么，以及安装命令。

在 Claude Code 梳理出这些内容之后，我用一个问题来筛选它们：

> 观众看完之后，能不能用一句话解释 Claude Code？

最后我确定的核心表达是：

> Claude Code 不是一个聊天框，而是一个能进入项目、理解代码、修改文件、运行命令的 AI 编码搭档。

我把这句话写进后续提示词，作为内容分析、视频叙事和 Scene 设计的共同判断标准。凡是不能帮助观众理解「它如何在项目里工作」的内容，我就要求 Claude Code 弱化处理，或者先放到后续视频中，不让它占太多画面时间。

因此，安装和登录的内容，我没有让 Claude Code 放进这一条视频，而是把它们留到了下一条视频。这一条先把 Claude Code 的基本概念和工作方式讲清楚。

## 工具选择

这条视频最终准备用 Remotion 实现，因为我希望视频中的场景、文字、时间轴和后续接入的音频都能通过代码控制，也方便以后复用基础场景。

一开始，我直接在 Remotion 里做了一个最小版本，先确认几件事能不能跑通：

- 工程能否启动。
- Composition 能否注册。
- Scene 能否按照时间播放。
- 文字、界面和简单动画能否正常出现。
- 能否在 Remotion Studio 中预览。

第一版先不考虑完整配音和字幕同步，重点是确认视觉结构是否成立。

在制作过程中我发现，如果实际做出来的效果与预期不一致，就需要反复向 AI 解释自己的想法。这样一来，反馈和修改会不断叠加，试错成本也比较高。于是，我又增加了一个无依赖的 HTML + CSS 静态原型。

在正式实现视频之前，我可以先通过静态原型看到 AI 对我描述的理解是否与预期一致。这个静态原型不是用来确认最终效果的，而是用来校准画面方向的。因此，它不需要做得太复杂，只要能够确认构图、信息密度和主要视觉方向基本正确即可。

这里第一次在 Remotion 中制作的最小版本，只是为了确认技术路线，并不是正式的视频实现。

最后，我的流程是：

```text
先用 Remotion 确认技术路线
→ 用 HTML + CSS 确认画面方向
→ 再回到 Remotion 做正式实现
```

## 整理原始内容

我没有一开始就让 Claude Code 把原始文章直接做成视频。这个阶段我的主要工作，也不是手动整理每一份文档，而是通过提示词告诉它：项目要解决什么问题、当前要完成哪一层资料、输出需要遵守哪些约束，以及完成后先停在哪里。

我先把原始文章和项目背景交给 Claude Code，让它先理解内容，不急着写代码。之后，我按照生产流程分阶段给提示词，让它依次整理：

- `source.md`：原始文章到底写了什么。
- `content-analysis.md`：这篇文章真正的核心观点和关系是什么。
- `video-narrative.md`：观众应该按照什么顺序理解。
- `scene-script.md`：需要拆成哪些 Scene，每个 Scene 解决什么问题。
- `narration-script.md`：每一幕具体怎么讲。
- `visual-script.md`：口播进行时，画面要发生什么。
- `visual-prototype.html`：画面结构和视觉方向是否成立。

我没有在一个提示词里要求 Claude Code 同时完成所有工作。每一步，我都会在提示词里明确这一阶段的目标、输入文件、输出文件、需要保留或删减的信息，以及完成后的确认标准。Claude Code 完成后，我先检查结果，再根据问题补充提示词，确认这一层没有明显偏差后，才让它继续下一层。

比如，我先让它分析原始内容的核心命题和知识关系，不让它马上拆 Scene；确认内容分析之后，我再让它根据这份分析重新组织视频叙事；叙事顺序确定后，再继续拆 Scene、写口播和设计画面。这样，前一个阶段的结果就会成为下一个阶段的明确输入。

在这个过程中，我主要负责提供背景、写提示词、做判断和给反馈；具体的内容分析、文档起草和结构整理则由 Claude Code 完成。我的工作重点不是替它把每个文件写出来，而是不断校准它对内容和制作目标的理解。

这样做以后，发现问题时比较容易定位：

- 如果你提取的核心观点不对，我就在提示词里纠正内容分析。
- 如果你安排的叙事顺序不对，我就要求你重新组织 `video-narrative.md`。
- 如果你拆出的某个 Scene 讲得太多，我就让你回到 `scene-script.md` 重新调整。
- 如果你理解的画面表达不对，我就继续修改 `visual-script.md` 或静态原型的要求。

这样一来，问题会被定位到具体的提示词和生产资料，而不是所有东西都混在代码里，改一个地方还不知道会影响什么。整个过程更像是我负责确定方向和不断校准，你负责按照提示词执行具体工作。

## 写口播和拆 Scene

这一部分，我没有先手动把原文改成口语，再让 Claude Code 配画面，而是通过提示词控制它的工作顺序：先从观众的认知变化重新组织叙事，再根据叙事拆分 Scene，最后基于已经确认的 Scene Script 写口播。

我在提示词里特别强调两点：不要按照原文章节机械地拆 Scene，也不要把功能列表直接搬到画面上；要先判断观众需要经历哪些变化，以及每个 Scene 具体要解决什么问题。Claude Code 生成初稿后，我会检查每一幕是否带来了新的理解、是否有清楚的转折，以及画面是否有机会展示过程。如果某一幕塞入了太多内容，我就继续补充提示词，让它重新拆分，而不是直接进入下一步。

这一条视频大致按照下面的顺序组织：

1. 先提出一个问题：Claude Code 到底是不是一个会写代码的聊天框。
2. 展示传统聊天式 AI 工作流为什么需要不断复制和粘贴。
3. 用同一个任务展示 Claude Code 如何直接进入项目执行。
4. 再解释 Agent 的工作方式。
5. 说明它可以理解项目、执行任务、连接外部工具。
6. 最后补上边界：AI 可以执行，但不能替我做最终判断。

确定叙事顺序之后，我再让 Claude Code 根据 Scene Script 生成 `narration-script.md`，而不是让它直接根据原始文章改写。这样可以让口播服务于已经确定的叙事和 Scene，而不是把原文换一种说法重新读一遍。

在这一步的提示词里，我会明确声音和画面的分工：口播负责解释意义、推进内容和做转折，画面负责展示具体过程和结果。也就是说，我让 Claude Code 先判断“这一句需要让观众理解什么”，再决定“画面应该证明什么”，而不是让画面重复口播内容。

比如口播说：

> Claude Code 可以直接参与项目修改。

画面就不再重复显示这句话，而是展示：

```text
auth.ts      Modified
user.ts      Modified
tests.ts     Created
```

我会在提示词里要求画面不要再显示「Claude Code 可以直接参与项目修改」这句话，而是展示文件状态的变化。这样，观众听到的是意义，看到的是证据。

## 先做一个能播放的版本

完成口播和 Scene 的初稿之后，我没有一开始就让 Claude Code 追求复杂的视觉效果，而是先在提示词里给它设定一个更小的目标：用已有的通用场景做出一版能播放的视频，先验证工程链路和场景配置是否成立。

我让 Claude Code 先复用这几种基础场景：

- 开场问题或标题。
- 概念解释。
- 左右对比。
- 步骤列表。
- 终端演示。
- 结尾总结。

在这一轮提示词里，我没有要求它马上确定最终视觉风格，而是要求它先把内容拆成多个 Scene，并让文字、界面和简单动画按照时间出现。这样做的目的，是先确认从生产资料到 Remotion 画面的基本链路能不能跑通。

我看完这一版后才发现，组件能够复用，并不代表画面已经设计好了。虽然每个 Scene 的内容不同，但画面却一直在重复标题、卡片、列表和标签。这个问题不是通过继续增加组件就能解决的，而是需要重新给 Claude Code 说明视觉表达的目标。

## 第一版的问题：视频像 PPT

第一版生成后，我先在预览里检查实际画面，再把问题整理成提示词反馈给 Claude Code。它的整体风格是深色背景、标题、字幕、卡片和列表，再加上一些淡入、位移和逐项出现的动画。

第一次完整预览时，我发现它确实能播放，但本质上只是把 PPT 页面按时间播放：

- 每个 Scene 都是标题加几张卡片。
- 口播说到什么，画面就把同样的文字显示出来。
- 元素只是一个接一个出现，没有真正的状态变化。
- Scene 之间像翻页，不像一个任务在继续推进。
- 画面展示了结论，却没有展示结论是怎样产生的。

我没有让 Claude Code 先去调整颜色或增加动画，而是先在提示词里指出：问题不是样式不够丰富，而是画面仍然停留在「会动的知识点页面」。后面的修改都围绕一个目标展开——让画面展示过程，而不是只展示结论。

### 口播和画面重复

比如口播说：

> Claude Code 会读取项目、修改文件、运行命令。

我在反馈中指出，如果画面同时出现「读取项目」「修改文件」「运行命令」三张卡片，这些文字只是把口播再写了一遍。

然后我要求 Claude Code 把两者分开：

- 口播说清楚这个能力有什么意义。
- 画面展示文件被读取、代码被修改、命令被执行。

### 一个 Scene 里内容太多

我在给 Claude Code 的第一轮 Scene 提示词里，仍然保留了几个比较大的内容段落，导致一个 Scene 同时放入多个知识点。比如「Claude Code 的能力」这一幕，同时讲理解项目、修改文件、运行命令和连接工具。

Claude Code 为了把这些内容都放进去，自然会增加更多卡片和标签，内容越完整，画面越像 PPT。

所以我在后续提示词中明确要求：一幕只完成一个主要认知变化，不要把一个完整章节塞进一屏。如果一个 Scene 同时承担多个任务，就先回到 `scene-script.md` 拆开，再继续修改画面。

### 动画只改变出现时间

早期的动画大多是：先出现标题，过几帧出现列表，再过几帧出现总结。

我把这类问题反馈给 Claude Code 后，要求它不要只改变元素的出现顺序，而要说明每个动作具体表达了什么：是扫描、连接、修改、执行，还是状态变化？

我也把一个简单的判断标准写进提示词：如果删掉动画之后，画面信息完全没有变化，那这个动画大概率只是装饰，需要改成能够表达过程的动作。

### 竖屏不适合展示这种过程

一开始，我也让 Claude Code 尝试过 `9:16` 竖屏。但当我要求画面同时展示文件树、代码编辑器、终端、任务状态和字幕时，横向空间很快就不够了。

我在预览中看到，继续缩小主体内容只会让信息更难读，或者把过程重新压缩成文字卡片。

所以我在后续提示词和项目约束中明确把这类工具教程调整成 `16:9` 横屏。画幅不只是输出设置，它会直接影响 Claude Code 能不能把文件树、代码、终端和状态变化同时放进画面里。

## 我先改两个场景

发现问题以后，我没有在提示词里要求 Claude Code 一次性把整条视频全部重做，而是先指定两个最能验证方向的场景作为试点。这样可以先确认「展示过程」的方向是否成立，再决定是否推广到其他 Scene。

### `Project Definition`

我先在提示词里说明，原来的画面只是用文字说明 Claude Code 可以读取项目、理解代码和修改文件，需要改成一个能够展示任务推进的模拟 IDE 工作区。

根据这个要求，Claude Code 把任务按顺序表现为：

1. 读取脚本和项目文件。
2. 找到当前场景或任务。
3. 修改配置或代码。
4. 运行检查。

我又补充限制，不需要制作完整 IDE，只保留文件树、代码区域和状态提示。这样既能控制实现范围，也足够让观众看见「它进入项目后做了什么」。

### `Human Review Gate`

第二个试点是 `Human Review Gate`。我在提示词里指出，原来的责任链卡片只是在说明「AI 执行，人来确认」，需要把它改成任务流程中的真实状态。

Claude Code 根据这个要求，把它改成终端执行日志，依次展示：

1. 读取项目。
2. 更新配置。
3. 运行检查。
4. 打开预览。
5. 停在 `Waiting for review`。

我检查这个场景时，重点看 `Waiting for review` 是否是前面执行过程自然产生的状态。这样「人必须把关」不再是最后突然出现的一句口号，而是任务流程中的一个真实节点。

我先只让 Claude Code 修改这两个场景，是为了确认一个问题：只要画面里出现真实任务、具体界面和状态变化，视频是不是就会从「展示观点」变成「展示过程」。

看过这两个场景之后，我根据预览结果确认这个方向成立，再让 Claude Code 继续考虑其他 Scene 是否需要用同样的方式调整。

## 静态 Visual Prototype

两个场景确认方向之后，我又通过提示词调整了后续制作顺序。

我要求 Claude Code 不要直接在 Remotion 里反复修改正式实现，而是先用无依赖的 HTML、CSS 和少量 JavaScript 做 `visual-prototype.html`，让我先看静态画面是否成立。

我在提示词里列出需要检查的内容：

- `16:9` 构图能不能容纳主体内容。
- 文件树、代码、终端和字幕会不会互相挤压。
- 每个 Scene 的主要视觉事件是否清楚。
- 文字是不是太多，观众能不能看完。
- 画面是不是又退回「标题 + 卡片」的形式。
- 不同 Scene 是否保持统一，同时又有不同的视觉机制。

同时，我也明确告诉 Claude Code，静态原型不需要还原最终动画，只要把画面结构、屏幕文字、状态变化和主要视觉事件展示清楚就够了。它的作用是让我判断 AI 是否正确理解了画面方向，而不是确认最终动画效果。

我看完静态原型后，如果发现画面本身就像 PPT，就继续修改提示词和静态原型，不让 Claude Code 直接进入正式 Remotion 实现；如果原型方向成立，再让它把已经确认的结构交给 Remotion 实现。这样可以避免在错误的结构上继续堆动画。

## 我的调整方法

这次制作让我形成了一个比较具体的提示词调整方法：不要每次都要求 Claude Code 把整条视频全部推倒重来，而是先找到最不满意的部分，只针对这一部分补充反馈。

比如：

- 如果一句口播太抽象，我就在提示词里先要求 Claude Code 改这一句，再让它判断画面应该演示什么。
- 如果一个 Scene 内容太多，我就让它先拆这个 Scene，不急着改其他场景。
- 如果一个画面像卡片，我就要求它增加一个真实任务或状态变化，而不是继续增加装饰。
- 如果整体构图有问题，我就让它先修改静态原型，不直接改正式 Remotion。

每次修改后，我先看局部结果；等几个局部都调整完之后，再从头完整看一遍，检查前后节奏和视觉风格是否一致。

我后来会用下面三个问题检查 Claude Code 产出的每个 Scene，这三个问题也会直接写进我的反馈和提示词里：

1. 这一幕让观众理解了什么新的东西？
2. 画面里发生了什么变化，而不是只显示了什么文字？
3. 如果去掉动画，这一幕是不是只剩一张信息卡？

如果答案只是「显示了几条结论」，我就继续要求 Claude Code 找一个更具体的过程：扫描、执行日志、Diff、连接、状态切换，或者一个需要人做决定的节点。

## 总结

到这里，我通过多轮提示词让 Claude Code 完成了第一版视频，也找到了「视频像 PPT」的原因。

我最后采用的方式，不是让 Claude Code 增加更多动画，而是通过提示词逐步完成几件事：

- 先确定视频要让观众理解的核心问题。
- 按阶段让 Claude Code 生成和修改生产资料，每一层确认后再进入下一层。
- 把口播和画面的职责分开。
- 把抽象能力改成具体的动作和状态。
- 一个 Scene 只表达一个主要认知变化。
- 先指定两个场景试验，再决定是否推广到其他 Scene。
- 在正式 Remotion 实现前，先做静态 Visual Prototype。
- 每次只针对具体问题补充提示词，最后再做全篇检查。

我最后得到的一个经验是：AI 能不能做出符合预期的视频，不只取决于它会不会写代码，也取决于我能不能把自己的判断拆成清楚、具体、可以检查的提示词。

> 我的工作不是把结论排版到画面上，而是通过提示词让 Claude Code 把结论转成观众能够看到的任务过程。

## 参考资料

- [视频生产流程](docs/VIDEO-PROJECT-WORKFLOW.md)
- [视频制作规则](docs/VIDEO-PRODUCTION-RULES.md)
- [视频制作问题与经验](docs/video-production-notes.md)
- [当前视频生产资料](videos/claude-code-what-is/)
